<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="zh-cn"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://quaner-dev.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://quaner-dev.github.io/" rel="alternate" type="text/html" hreflang="zh-cn"/><updated>2026-08-19T04:00:24+00:00</updated><id>https://quaner-dev.github.io/feed.xml</id><title type="html">Quaner’s Blog</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">AI 在工作中的使用边界：找到真正有价值的甜蜜区间</title><link href="https://quaner-dev.github.io/blog/2026/AI%E5%9C%A8%E5%B7%A5%E4%BD%9C%E4%B8%AD%E7%9A%84%E4%BD%BF%E7%94%A8%E8%BE%B9%E7%95%8C-%E6%89%BE%E5%88%B0%E7%9C%9F%E6%AD%A3%E6%9C%89%E4%BB%B7%E5%80%BC%E7%9A%84%E7%94%9C%E8%9C%9C%E5%8C%BA%E9%97%B4/" rel="alternate" type="text/html" title="AI 在工作中的使用边界：找到真正有价值的甜蜜区间"/><published>2026-08-19T00:00:00+00:00</published><updated>2026-08-19T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2026/AI%E5%9C%A8%E5%B7%A5%E4%BD%9C%E4%B8%AD%E7%9A%84%E4%BD%BF%E7%94%A8%E8%BE%B9%E7%95%8C-%E6%89%BE%E5%88%B0%E7%9C%9F%E6%AD%A3%E6%9C%89%E4%BB%B7%E5%80%BC%E7%9A%84%E7%94%9C%E8%9C%9C%E5%8C%BA%E9%97%B4</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2026/AI%E5%9C%A8%E5%B7%A5%E4%BD%9C%E4%B8%AD%E7%9A%84%E4%BD%BF%E7%94%A8%E8%BE%B9%E7%95%8C-%E6%89%BE%E5%88%B0%E7%9C%9F%E6%AD%A3%E6%9C%89%E4%BB%B7%E5%80%BC%E7%9A%84%E7%94%9C%E8%9C%9C%E5%8C%BA%E9%97%B4/"><![CDATA[<p>最近在实际使用 AI 的过程中，我越来越觉得：<strong>AI 是否有价值，并不取决于它能不能把一个任务做到 100%。</strong></p> <p>相比“完整替代人”，我现在更关心的是另一件事：</p> <blockquote> <p><strong>AI 能不能用很低的成本，快速完成这个任务中最耗时间的部分。</strong></p> </blockquote> <p>如果一个原来需要很长时间完成的工作，AI 能快速完成其中 60%、70% 或 80%，剩余部分由人很快收尾，那么它已经产生了很高的价值。</p> <p>这也是我在实际使用 OpenClaw 处理工作任务以后，逐渐形成的一种感受。</p> <hr/> <h2 id="一这个甜蜜区间是从实际使用中发现的">一、这个“甜蜜区间”是从实际使用中发现的</h2> <p>我在 OpenClaw 里做过一类比较典型的工作：<strong>对点位、设备数据进行整理和清洗。</strong></p> <p>这类任务本身有一定规则，但原始数据又不是完全规整的。如果全部人工处理，需要花很多时间逐条判断和整理。</p> <p>一开始很自然地会想到，让 AI 直接理解数据，然后通过 Prompt 去做提取、分类和整理。</p> <p>这种方式当然可以做，而且效果可能并不差。</p> <p>但真正使用以后会发现一个问题：</p> <p><strong>让 AI 做到“基本可用”并不困难，让它做到“非常稳定、接近完美”却越来越贵。</strong></p> <p>比如一个数据整理任务，AI 很快就可以完成其中 60%～80%，这时候效率非常高。大部分机械性的整理工作已经被解决，我只需要处理少量异常情况。</p> <p>但如果继续要求：</p> <blockquote> <p>90% 呢？<br/> 95% 呢？<br/> 能不能每一次都完全正确？</p> </blockquote> <p>事情就开始变化。</p> <p>为了提高最后这些准确度，可能需要不断调整 Prompt、补充规则、增加样例、重新测试，然后再处理新的边界情况。</p> <p>最后会发现：</p> <blockquote> <p><strong>提升的准确率越来越少，但付出的时间越来越多。</strong></p> </blockquote> <p>这就是我真正开始意识到“甜蜜区间”的地方。</p> <pre><code class="language-mermaid">flowchart LR
    A["开始处理任务"] --&gt; B["60%～70%"]
    B --&gt; C["70%～80%"]
    C --&gt; D["80%～90%"]
    D --&gt; E["90%～95%+"]
    E --&gt; F["追求接近100%"]

    B --- B1["很快"]
    C --- C1["高收益"]
    D --- D1["通常仍然划算"]
    E --- E1["优化成本明显增加"]
    F --- F1["可能得不偿失"]

    style C fill:#d9f7be,stroke:#52c41a,stroke-width:3px
    style D fill:#e6f7ff,stroke:#1890ff
    style E fill:#fff1b8,stroke:#faad14
    style F fill:#ffccc7,stroke:#f5222d
</code></pre> <p>这里的百分比当然不是一个严格的数学指标，更接近实际使用中的主观感受。</p> <p>但这种趋势很明显：</p> <blockquote> <p><strong>AI 前面的大部分工作往往完成得非常快，越接近“完美”，边际成本越高。</strong></p> </blockquote> <hr/> <h2 id="二有时候最好的-ai-用法是让-ai-帮我写程序">二、有时候最好的 AI 用法，是让 AI 帮我写程序</h2> <p>点位数据整理这个实践还有另外一个很有意思的地方。</p> <p>如果一直让模型通过 Prompt 对数据逐条理解、提取和判断，本质上每一次都在重新进行一次推理。</p> <p>它能够做到，但速度、稳定性以及后续优化成本未必是最优的。</p> <p>后来我会采用另外一种方式：</p> <p><strong>先让 AI 理解这个问题，再让 AI 自己把已经能够明确描述的规则写成脚本。</strong></p> <p>然后由脚本真正进行批量处理。</p> <pre><code class="language-mermaid">flowchart LR
    A["点位 / 设备原始数据"] --&gt; B["AI理解问题"]
    B --&gt; C["归纳整理规则"]
    C --&gt; D["AI生成处理脚本"]
    D --&gt; E["脚本批量处理"]
    E --&gt; F["人工检查少量异常"]

    style B fill:#e6f7ff,stroke:#1890ff
    style D fill:#d9f7be,stroke:#52c41a
    style E fill:#d9f7be,stroke:#52c41a
</code></pre> <p>这种方式给我的感受反而更好。</p> <p>因为对于 AI 来说，生成一个用于数据清洗的脚本其实很快。一旦规则能够程序化，真正执行的时候就没有必要继续让模型逐条判断。</p> <p>它可能并没有处理掉所有特殊情况，但只要能够覆盖大部分正常数据，比如做到 80%～90% 的有效处理，实际上已经非常划算。</p> <p>剩余少量异常由人处理，往往比继续花大量时间调 Prompt 更高效。</p> <p>所以我逐渐发现：</p> <blockquote> <p><strong>AI 的价值不一定是自己把所有数据处理完，而是快速帮我找到一种能够解决大部分问题的方法。</strong></p> </blockquote> <p>这两者其实有很大的区别。</p> <p>如果最终目标是完成工作，那么没有必要执着于“必须由 AI 自己完成”。</p> <p>AI 帮我写出一个脚本，脚本完成 80%～90%，我再处理剩余部分，同样是 AI 带来的效率提升。</p> <p>甚至很多时候，这是更好的使用方式。</p> <hr/> <h2 id="三ai-的边界本质上还是投入产出比">三、AI 的边界，本质上还是投入产出比</h2> <p>这种现象并不仅仅存在于数据清洗中。</p> <p>在代码修改、脚本生成、问题排查、局部重构这类小型和中型任务中，我也有类似的感受。</p> <p>这些任务通常：</p> <ul> <li>范围比较明确；</li> <li>上下文比较有限；</li> <li>结果比较容易检查；</li> <li>AI 做完以后，人很容易继续接手。</li> </ul> <p>因此，即使 AI 没有做到 100%，依然能够明显降低工作时间。</p> <p>而随着任务越来越复杂，情况会逐渐发生变化。</p> <pre><code class="language-mermaid">flowchart LR
    A["小型任务"] --&gt; B["中型任务"]
    B --&gt; C["大型任务"]

    A --- A1["结果容易判断&lt;br/&gt;AI收益明显"]
    B --- B1["通常是高杠杆区域"]
    C --- C1["同样可以使用AI&lt;br/&gt;但偏离和校正成本上升"]

    style A fill:#e6f7ff,stroke:#1890ff
    style B fill:#d9f7be,stroke:#52c41a,stroke-width:3px
    style C fill:#fff1b8,stroke:#faad14
</code></pre> <p>这并不意味着大型任务不能使用 AI。</p> <p>比如：</p> <ul> <li>架构设计；</li> <li>技术方案；</li> <li>项目规划；</li> <li>模块拆解；</li> <li>大规模重构思路；</li> </ul> <p>AI 同样可以提供很大的帮助。</p> <p>只是大型任务包含的上下文和决策更多，执行链路也更长。AI 在前面产生的一个小偏差，到了后面可能被逐渐放大。</p> <p>所以大型任务里面，人的作用会更加明显。</p> <p>可能不是：</p> <blockquote> <p>把任务完整交出去，最后只看结果。</p> </blockquote> <p>而是：</p> <blockquote> <p><strong>AI 帮助分析、规划和执行，人不断判断方向有没有发生偏离。</strong></p> </blockquote> <p>因此真正的边界并不是简单地划成：</p> <blockquote> <p>小任务可以用 AI，大任务不能用 AI。</p> </blockquote> <p>而应该是：</p> <blockquote> <p><strong>随着任务复杂度增加，对人的判断和介入要求也会增加。</strong></p> </blockquote> <hr/> <h2 id="结语真正重要的是找到-ai-最有杠杆率的位置">结语：真正重要的是找到 AI 最有杠杆率的位置</h2> <p>经过这些实际使用以后，我现在越来越不在意 AI 是否真正完成了“100%”。</p> <p>我更在意的是：</p> <blockquote> <p><strong>它帮我省了多少时间。</strong></p> </blockquote> <p>如果一个任务 AI 做到 70%，我十分钟就能补完，那么这次 AI 使用就是成功的。</p> <p>如果 AI 已经做到 95%，但为了最后 5% 还需要投入大量时间去调 Prompt、检查和返工，那就可能已经不划算了。</p> <p>所以我现在理解的“AI 甜蜜区间”，其实就是：</p> <blockquote> <p><strong>AI 节省的时间最多，而人为了管理、检查和纠正 AI 所付出的成本还很低的那一段。</strong></p> </blockquote> <p>这也是为什么真正会使用 AI，可能不只是“会不会写 Prompt”或者“会不会用 Agent”。</p> <p>更重要的是一种判断力，甚至可以说是一种 <strong>AI 使用品位</strong>：</p> <p>知道什么事情应该交给 AI；</p> <p>知道应该让 AI 做到什么程度；</p> <p>知道什么时候继续优化已经没有意义；</p> <p>也知道什么时候应该自己接手，或者直接让 AI 写一个普通程序来解决。</p> <p>所以我目前对 AI 工作边界的理解，可以暂时归结成一句话：</p> <blockquote> <p><strong>AI 的价值不在于把所有事情做到 100%，而在于找到它投入最少、收益最大的那一段。</strong></p> </blockquote> <p>真正高效的 AI 使用，不是追求“全部 AI 化”，而是不断找到自己工作中那些 <strong>AI 杠杆率最高的甜蜜区间</strong>。</p>]]></content><author><name></name></author><category term="AI"/><category term="AI"/><category term="工作效率"/><category term="OpenClaw"/><summary type="html"><![CDATA[从数据清洗、脚本生成和任务复杂度出发，思考 AI 使用中投入最少、收益最大的甜蜜区间。]]></summary></entry><entry><title type="html">AI 时代，重新思考项目交付的边界</title><link href="https://quaner-dev.github.io/blog/2026/AI%E6%97%B6%E4%BB%A3-%E9%87%8D%E6%96%B0%E6%80%9D%E8%80%83%E9%A1%B9%E7%9B%AE%E4%BA%A4%E4%BB%98%E7%9A%84%E8%BE%B9%E7%95%8C/" rel="alternate" type="text/html" title="AI 时代，重新思考项目交付的边界"/><published>2026-08-19T00:00:00+00:00</published><updated>2026-08-19T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2026/AI%E6%97%B6%E4%BB%A3-%E9%87%8D%E6%96%B0%E6%80%9D%E8%80%83%E9%A1%B9%E7%9B%AE%E4%BA%A4%E4%BB%98%E7%9A%84%E8%BE%B9%E7%95%8C</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2026/AI%E6%97%B6%E4%BB%A3-%E9%87%8D%E6%96%B0%E6%80%9D%E8%80%83%E9%A1%B9%E7%9B%AE%E4%BA%A4%E4%BB%98%E7%9A%84%E8%BE%B9%E7%95%8C/"><![CDATA[<p>最近在实际项目交付过程中，我一直在思考一个问题：</p> <blockquote> <p><strong>随着 AI 开发能力越来越强，传统的产品、研发和交付之间的边界，会不会发生变化？</strong></p> </blockquote> <p>这个思考和我当前所处的工作环境有很直接的关系。</p> <p>我们目前整体上仍然是一种<strong>主产品驱动</strong>的模式。前场团队直接面对客户，除了实施交付以外，通常还会承担需求沟通、项目管理、问题解决，以及一定程度的售前和方案工作。</p> <p>与此同时，真实项目中又不可避免地会出现大量个性化需求。</p> <p>过去面对这类需求，通常需要经过一条比较完整的链路：</p> <pre><code class="language-mermaid">flowchart LR
    A[客户需求] --&gt; B[前场收集与整理]
    B --&gt; C[产品分析与细化]
    C --&gt; D[研发评估与排期]
    D --&gt; E[开发与测试]
    E --&gt; F[前场验证]
    F --&gt; G[项目交付]
</code></pre> <p>这套模式本身没有问题，但它背后有一个重要前提：</p> <blockquote> <p><strong>研发能力是一种相对稀缺的资源。</strong></p> </blockquote> <p>因此，对于一些只服务于少数项目、暂时又无法沉淀为标准产品的“小而具体”的需求，往往会出现研发投入与项目收益不匹配的问题。</p> <p>而 AI 的出现，正在改变这个前提。</p> <hr/> <h2 id="一ai-让一部分研发能力开始向前场延伸">一、AI 让一部分研发能力开始向前场延伸</h2> <p>随着 AI 编程能力的发展，一个具备一定技术基础，同时理解产品和客户场景的人，已经能够完成一部分过去必须依赖专业研发人员才能完成的工作，例如：</p> <ul> <li>项目脚本和自动化工具；</li> <li>数据转换和系统对接；</li> <li>特定接口适配；</li> <li>独立的小型服务或模块；</li> <li>场景化的数据处理逻辑。</li> </ul> <p>这并不意味着前场要替代研发。</p> <p>真正发生的变化是：</p> <blockquote> <p><strong>前场开始具备把客户场景快速转换成“可运行方案”的能力。</strong></p> </blockquote> <p>过去的交付更多承担“需求传递”和“产品落地”，未来则可能进一步参与：</p> <pre><code class="language-mermaid">flowchart LR
    A[客户场景] --&gt; B[理解与拆解需求]
    B --&gt; C[判断已有产品能力]
    C --&gt; D[选择实现方式]
    D --&gt; E[AI 辅助快速实现]
    E --&gt; F[项目现场验证]
</code></pre> <p>这样，一部分个性化需求不需要一开始就进入完整的正式研发流程，而可以先低成本验证其价值。</p> <hr/> <h2 id="二前场可以开发但必须和主产品建立边界">二、前场可以开发，但必须和主产品建立边界</h2> <p>AI 降低了代码生产成本，但并没有降低软件长期维护的复杂度。</p> <p>如果前场具备开发能力以后，每个项目都直接修改主产品，最终很容易形成大量客户分支和特殊逻辑。短期看响应速度提高了，长期却会带来版本升级困难、代码耦合和维护成本失控。</p> <p>因此，我更倾向于一个原则：</p> <blockquote> <p><strong>核心产品保持稳定，项目变化尽可能外置。</strong></p> </blockquote> <p>可以把整体能力简单划分为三层：</p> <pre><code class="language-mermaid">flowchart TB
    A[核心产品层&lt;br/&gt;产品与研发负责] --&gt; B[扩展能力层&lt;br/&gt;标准接口与插件机制]
    B --&gt; C[项目场景层&lt;br/&gt;前场快速实现与验证]

    A1[核心架构 / 数据模型 / 性能 / 安全 / 主线能力]
    B1[插件 / Adapter / API / Webhook / 规则组件 / SDK]
    C1[脚本 / 对接件 / 独立小模块 / 项目级定制]

    A --- A1
    B --- B1
    C --- C1
</code></pre> <p>其中：</p> <ul> <li><strong>核心产品层</strong>：继续由正式产品和研发团队负责，保证架构、性能、安全和长期版本演进；</li> <li><strong>扩展能力层</strong>：由产品提供标准化扩展机制，为项目定制建立明确边界；</li> <li><strong>项目场景层</strong>：前场可以借助 AI 快速开发脚本、插件、小模块和项目级能力。</li> </ul> <p>这里的核心不是“让前场去改产品”，而是让前场拥有一个<strong>受控、可维护、可沉淀的开发空间</strong>。</p> <hr/> <h2 id="三项目定制可以成为产品能力的前置验证">三、项目定制可以成为产品能力的前置验证</h2> <p>如果这种模式能够建立起来，它的价值就不只是减少研发压力。</p> <p>更重要的是，它可能改变产品需求的验证方式。</p> <p>过去通常是：</p> <pre><code class="language-mermaid">flowchart LR
    A[客户提出需求] --&gt; B[产品判断]
    B --&gt; C[研发投入]
    C --&gt; D[项目验证]
</code></pre> <p>本质上是：<strong>先投入，再验证。</strong></p> <p>未来可以增加另一条路径：</p> <pre><code class="language-mermaid">flowchart LR
    A[客户提出场景] --&gt; B[前场快速实现]
    B --&gt; C[项目验证]
    C --&gt; D{是否具有通用价值}

    D -- 一次性 --&gt; E[保留为项目能力]
    D -- 可复用 --&gt; F[沉淀为插件/组件]
    D -- 高度通用 --&gt; G[产品化评审]
    G --&gt; H[进入主产品]
</code></pre> <p>这样，一个项目需求最终可以有三种去向：</p> <ol> <li><strong>一次性项目需求</strong>：保留为项目脚本、小工具或项目模块；</li> <li><strong>重复出现但存在差异的需求</strong>：沉淀为插件、组件或标准对接件；</li> <li><strong>经过多个项目验证的共性需求</strong>：由产品和研发重新设计，正式进入主产品。</li> </ol> <p>最终形成一条新的产品演进路径：</p> <blockquote> <p><strong>客户场景 → 项目验证 → 可复用能力 → 产品能力</strong></p> </blockquote> <p>这时，项目交付不再只是产品生命周期的最后一环，也开始成为产品能力孵化的入口。</p> <hr/> <h2 id="四前场角色也会随之发生变化">四、前场角色也会随之发生变化</h2> <p>结合当前的实际工作来看，前场本身已经承担了比较综合的职责，包括：</p> <ul> <li>客户沟通；</li> <li>需求理解；</li> <li>项目管理；</li> <li>实施交付；</li> <li>问题排查；</li> <li>一部分技术定制。</li> </ul> <p>如果继续沿着这个方向发展，未来前场人员的能力模型可能进一步变成：</p> <pre><code class="language-mermaid">mindmap
  root((前场能力模型))
    业务理解
    产品理解
    项目管理
    技术能力
      API
      数据处理
      脚本与插件
      基础架构
    AI能力
      需求上下文构建
      AI辅助开发
      调试与验证
      代码质量判断
</code></pre> <p>这里最重要的并不是把交付人员培养成专业研发，而是让他具备几种关键判断能力：</p> <ul> <li>能把客户场景拆成具体的产品和技术问题；</li> <li>能判断什么可以通过配置解决；</li> <li>能判断什么适合脚本、插件或小模块快速验证；</li> <li>能判断什么必须进入正式产品研发；</li> <li>能把已经验证有效的项目能力重新抽象出来。</li> </ul> <p>从角色定位上看，这已经开始接近 <strong>Solution Engineer / FDE（Forward Deployed Engineer）</strong> 的一些特点。</p> <p>但对我们当前阶段来说，更现实的方式并不是立即建立一个完全独立的新岗位，而是：</p> <blockquote> <p><strong>先把这种能力逐步融合到现有交付体系中。</strong></p> </blockquote> <hr/> <h2 id="五我理解的一条现实演进路径">五、我理解的一条现实演进路径</h2> <p>结合现有的组织和产品形态，我认为更合理的方式是逐步演进，而不是一次性重构。</p> <pre><code class="language-mermaid">flowchart LR
    A[阶段1&lt;br/&gt;AI增强型交付]
    --&gt; B[阶段2&lt;br/&gt;插件化/扩展化交付]
    --&gt; C[阶段3&lt;br/&gt;项目能力沉淀]
    --&gt; D[阶段4&lt;br/&gt;形成新的解决方案角色]
</code></pre> <h3 id="阶段-1ai-增强型交付">阶段 1：AI 增强型交付</h3> <p>首先让 AI 服务于现有工作：</p> <ul> <li>编写项目脚本；</li> <li>数据处理；</li> <li>接口适配；</li> <li>辅助调试；</li> <li>自动生成测试；</li> <li>项目文档。</li> </ul> <h3 id="阶段-2插件化--扩展化交付">阶段 2：插件化 / 扩展化交付</h3> <p>逐步建立主产品之外的扩展能力：</p> <ul> <li>插件；</li> <li>标准对接件；</li> <li>独立小模块；</li> <li>通用脚本库；</li> <li>扩展 API 和 SDK。</li> </ul> <h3 id="阶段-3建立项目能力沉淀机制">阶段 3：建立项目能力沉淀机制</h3> <p>把项目产生的能力进行分类：</p> <ul> <li>一次性项目能力；</li> <li>可复用项目能力；</li> <li>产品候选能力。</li> </ul> <p>让项目定制不再只是“做完即结束”，而能够形成持续积累。</p> <h3 id="阶段-4形成新的解决方案角色">阶段 4：形成新的解决方案角色</h3> <p>当产品边界、技术规范和沉淀机制逐渐成熟以后，前场角色才可能真正演进成更加完整的 Solution Engineer / FDE 类型角色。</p> <hr/> <h2 id="结语">结语</h2> <p>所以，我现在对 AI 和项目交付的理解，已经不太局限于“AI 能不能帮助交付人员提高效率”。</p> <p>更值得关注的是：</p> <blockquote> <p><strong>当开发能力不再像过去一样高度稀缺以后，公司是否可以重新划分产品、研发和前场之间的边界。</strong></p> </blockquote> <p>我理解的一种可能方向是：</p> <ul> <li><strong>研发</strong>负责把核心平台和长期能力做深；</li> <li><strong>前场</strong>依靠对客户和业务场景的理解，把场景做宽，并借助 AI 快速完成验证；</li> <li><strong>产品</strong>负责从已经验证的项目能力中完成抽象和标准化。</li> </ul> <p>可以把这种关系概括为：</p> <pre><code class="language-mermaid">flowchart LR
    A[前场&lt;br/&gt;发现并验证场景]
    --&gt; B[产品&lt;br/&gt;抽象与标准化]
    --&gt; C[研发&lt;br/&gt;工程化与平台化]
    --&gt; D[正式产品能力]
</code></pre> <p>过去，项目交付的重点是：</p> <blockquote> <p><strong>把已有产品交付给客户。</strong></p> </blockquote> <p>未来，它可能还会多承担一项工作：</p> <blockquote> <p><strong>在交付过程中，持续发现、验证并孵化下一步的产品能力。</strong></p> </blockquote> <p>这可能是 AI 时代项目交付值得探索的一种新形态。</p>]]></content><author><name></name></author><category term="AI"/><category term="AI"/><category term="项目交付"/><category term="产品研发"/><summary type="html"><![CDATA[探讨 AI 如何让研发能力向前场延伸，并重新划分产品、研发与项目交付之间的边界。]]></summary></entry><entry><title type="html">Linux tmpfs 内存盘挂载</title><link href="https://quaner-dev.github.io/blog/2026/Linux-tmpfs%E5%86%85%E5%AD%98%E7%9B%98%E6%8C%82%E8%BD%BD/" rel="alternate" type="text/html" title="Linux tmpfs 内存盘挂载"/><published>2026-07-06T00:00:00+00:00</published><updated>2026-07-06T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2026/Linux-tmpfs%E5%86%85%E5%AD%98%E7%9B%98%E6%8C%82%E8%BD%BD</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2026/Linux-tmpfs%E5%86%85%E5%AD%98%E7%9B%98%E6%8C%82%E8%BD%BD/"><![CDATA[<h2 id="tmpfs-简介">tmpfs 简介</h2> <p><code class="language-plaintext highlighter-rouge">tmpfs</code> 是 Linux 提供的一种基于内存的临时文件系统，可以把一块内存空间挂载成目录使用。</p> <p>它和普通磁盘目录的区别是：</p> <ul> <li>读写速度快，适合临时文件、高频读写缓存、构建目录等场景</li> <li>数据保存在内存中，重启、关机或卸载后会丢失</li> <li>实际占用空间会按需使用，不是挂载多大就立刻占用多少内存</li> <li>如果写入的数据超过限制，可能会影响系统内存，需要合理设置大小</li> </ul> <h2 id="临时挂载内存盘">临时挂载内存盘</h2> <p>下面示例将 <code class="language-plaintext highlighter-rouge">tmpfs</code> 挂载到 <code class="language-plaintext highlighter-rouge">/mnt/ramdisk</code>，大小限制为 <code class="language-plaintext highlighter-rouge">2G</code>。</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 创建挂载目录</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> /mnt/ramdisk

<span class="c"># 挂载 tmpfs</span>
mount <span class="nt">-t</span> tmpfs <span class="nt">-o</span> <span class="nv">size</span><span class="o">=</span>2G tmpfs /mnt/ramdisk

<span class="c"># 查看挂载结果</span>
<span class="nb">df</span> <span class="nt">-h</span> /mnt/ramdisk
</code></pre></div></div> <h2 id="卸载内存盘">卸载内存盘</h2> <p>卸载前需要确认没有进程正在使用该目录。</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>umount /mnt/ramdisk
</code></pre></div></div> <p>如果提示目录正在使用，可以通过下面命令查看占用进程：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>fuser <span class="nt">-vm</span> /mnt/ramdisk
</code></pre></div></div> <h2 id="开机自动挂载">开机自动挂载</h2> <p>如果希望系统启动后自动挂载，可以写入 <code class="language-plaintext highlighter-rouge">/etc/fstab</code>。</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vim /etc/fstab
</code></pre></div></div> <p>添加下面一行：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tmpfs /mnt/ramdisk tmpfs defaults,size<span class="o">=</span>2G 0 0
</code></pre></div></div> <p>参数说明：</p> <table> <thead> <tr> <th>参数</th> <th>说明</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">tmpfs</code></td> <td>文件系统类型</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">/mnt/ramdisk</code></td> <td>挂载目录</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">defaults</code></td> <td>使用默认挂载参数</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">size=2G</code></td> <td>限制最大可使用容量为 2G</td> </tr> </tbody> </table> <p>配置完成后，不需要重启，可以直接测试挂载配置：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mount <span class="nt">-a</span>
<span class="nb">df</span> <span class="nt">-h</span> /mnt/ramdisk
</code></pre></div></div>]]></content><author><name></name></author><category term="Shell"/><category term="tmpfs"/><category term="ramdisk"/><category term="mount"/><summary type="html"><![CDATA[tmpfs 简介]]></summary></entry><entry><title type="html">Python 父进程和子进程的关系</title><link href="https://quaner-dev.github.io/blog/2026/Python%E7%88%B6%E8%BF%9B%E7%A8%8B%E5%92%8C%E5%AD%90%E8%BF%9B%E7%A8%8B%E7%9A%84%E5%85%B3%E7%B3%BB/" rel="alternate" type="text/html" title="Python 父进程和子进程的关系"/><published>2026-07-05T00:00:00+00:00</published><updated>2026-07-05T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2026/Python%E7%88%B6%E8%BF%9B%E7%A8%8B%E5%92%8C%E5%AD%90%E8%BF%9B%E7%A8%8B%E7%9A%84%E5%85%B3%E7%B3%BB</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2026/Python%E7%88%B6%E8%BF%9B%E7%A8%8B%E5%92%8C%E5%AD%90%E8%BF%9B%E7%A8%8B%E7%9A%84%E5%85%B3%E7%B3%BB/"><![CDATA[<h2 id="父进程和子进程parent-process-and-child-process">父进程和子进程（Parent Process and Child Process）</h2> <p>一个进程在运行过程中，可以创建新的进程。创建者称为父进程，被创建出来的进程称为子进程。</p> <p>在 Python 中，常见的创建子进程方式有：</p> <ul> <li><code class="language-plaintext highlighter-rouge">multiprocessing</code></li> <li><code class="language-plaintext highlighter-rouge">subprocess</code></li> <li><code class="language-plaintext highlighter-rouge">os.fork</code>，主要用于 Unix / Linux 系统</li> </ul> <p>父进程和子进程是两个独立的进程。它们有各自的进程 ID、内存空间和执行流程。</p> <h3 id="生命周期">生命周期</h3> <p>父子进程的生命周期可以简单理解为：</p> <pre><code class="language-mermaid">sequenceDiagram
    participant C as 子进程
    participant P as 父进程

    P--&gt;&gt;C: 创建并启动子进程

    par 并发执行
        C-&gt;&gt;C: 执行子进程任务
    and
        P-&gt;&gt;P: 执行父进程逻辑
    end

    alt 父进程等待子进程
        P--&gt;&gt;C: 等待子进程结束
        C--&gt;&gt;P: 返回退出状态
        P-&gt;&gt;P: 回收子进程
    else 父进程不等待子进程
        P-&gt;&gt;P: 继续执行自己的逻辑
    end
</code></pre> <h3 id="创建阶段">创建阶段</h3> <p>父进程创建子进程后，子进程会拥有自己的运行环境。</p> <p>在操作系统层面，子进程通常会继承父进程的一部分信息，例如环境变量、工作目录、打开的文件描述符等。但子进程不是父进程中的一个函数调用，它是一个独立运行的进程。</p> <p>在 Python 的 <code class="language-plaintext highlighter-rouge">multiprocessing</code> 中，父进程通常会先创建 <code class="language-plaintext highlighter-rouge">Process</code> 对象，然后通过 <code class="language-plaintext highlighter-rouge">start()</code> 启动子进程。</p> <p>整体流程是：</p> <ul> <li>父进程创建 <code class="language-plaintext highlighter-rouge">Process</code> 对象。</li> <li><code class="language-plaintext highlighter-rouge">start()</code> 启动子进程。</li> <li>子进程执行指定任务。</li> <li><code class="language-plaintext highlighter-rouge">join()</code> 让父进程等待子进程执行结束。</li> </ul> <h3 id="执行阶段">执行阶段</h3> <p>子进程启动后，会和父进程并发执行。</p> <p>如果父进程没有等待子进程，父进程会继续执行自己的代码。子进程则在自己的执行流程中运行，不会阻塞父进程。</p> <h3 id="等待阶段">等待阶段</h3> <p>如果父进程需要获取子进程执行完成的时机，就需要等待子进程。</p> <p>在 Python 中，常用 <code class="language-plaintext highlighter-rouge">join()</code> 等待子进程结束。<code class="language-plaintext highlighter-rouge">join()</code> 的作用是阻塞父进程，直到对应的子进程执行结束。</p> <p>如果不等待子进程，父进程可能会先执行完自己的逻辑。</p> <h3 id="退出阶段">退出阶段</h3> <p>父进程退出后，子进程是否继续执行，取决于子进程的创建方式和运行环境。</p> <p>不同的退出顺序会产生不同的进程状态，例如孤儿进程、僵尸进程、守护进程。</p> <h2 id="孤儿进程orphan-process">孤儿进程（Orphan Process）</h2> <p>可以简单理解为：</p> <pre><code class="language-mermaid">sequenceDiagram
    participant C as 子进程
    participant P as 父进程
    participant I as init / systemd

    P--&gt;&gt;C: 创建并启动子进程

    par 并发执行
        C-&gt;&gt;C: 继续执行自己的任务
    and
        P-&gt;&gt;P: 执行父进程逻辑
    end

    P-&gt;&gt;P: 父进程先退出
    C-&gt;&gt;C: 子进程仍在运行
    I--&gt;&gt;C: 接管孤儿进程
    C-&gt;&gt;C: 父进程 ID 变为 init / systemd
</code></pre> <p>孤儿进程指的是：父进程已经退出，但子进程仍然在运行的进程。</p> <p>它“孤儿”的原因是：创建它的原父进程已经结束，但它自己还没有结束。</p> <p>孤儿进程不会一直处于没有父进程的状态。系统会把它交给初始化进程接管，在 Linux 中通常是 PID 为 1 的 <code class="language-plaintext highlighter-rouge">init</code> 或 <code class="language-plaintext highlighter-rouge">systemd</code>。接管之后，孤儿进程的父进程 ID 会变成初始化进程的 PID。</p> <p>孤儿进程本身不一定是问题。如果子进程本来就需要独立完成任务，那么父进程退出后它继续运行是可以接受的。</p> <h2 id="僵尸进程zombie-process">僵尸进程（Zombie Process）</h2> <p>可以简单理解为：</p> <pre><code class="language-mermaid">sequenceDiagram
    participant C as 子进程
    participant OS as 操作系统
    participant P as 父进程

    P--&gt;&gt;C: 创建并启动子进程

    par 并发执行
        C-&gt;&gt;C: 执行子进程任务
    and
        P-&gt;&gt;P: 暂未 join / wait
    end

    C--&gt;&gt;OS: 子进程执行结束
    OS-&gt;&gt;OS: 保留退出状态和进程表记录
    OS--&gt;&gt;P: 等待父进程读取退出状态
    Note over C,OS: 子进程已经结束，但记录还没被回收
    P--&gt;&gt;OS: join / wait 读取退出状态
    OS-&gt;&gt;OS: 回收子进程记录
</code></pre> <p>僵尸进程指的是：子进程已经结束，但父进程还没有读取它的退出状态。</p> <p>子进程退出后，操作系统需要保留一些信息给父进程读取，例如：</p> <ul> <li>子进程的 PID</li> <li>子进程的退出码</li> <li>子进程的资源使用情况</li> </ul> <p>在父进程读取这些信息之前，子进程虽然已经不再运行，但进程表中仍然保留着它的记录，这种状态就是僵尸进程。</p> <p>僵尸进程不是还在运行的进程，它不会继续占用 CPU，但会占用进程表记录。如果大量僵尸进程长期存在，可能会影响系统继续创建新进程。</p> <p>在 Python 中，使用 <code class="language-plaintext highlighter-rouge">multiprocessing</code> 时通常通过 <code class="language-plaintext highlighter-rouge">join()</code> 等待并回收子进程。</p> <h2 id="守护进程daemon-process">守护进程（Daemon Process）</h2> <p>可以简单理解为：</p> <pre><code class="language-mermaid">sequenceDiagram
    participant C as 守护子进程
    participant P as 父进程
    participant OS as 操作系统

    P--&gt;&gt;C: 创建守护子进程 daemon=True

    par 依附父进程运行
        C-&gt;&gt;C: 执行辅助任务
    and
        P-&gt;&gt;P: 执行主任务
    end

    P-&gt;&gt;P: 父进程准备退出
    P--&gt;&gt;C: 通知守护子进程结束
    OS--&gt;&gt;C: 守护子进程通常被终止
    P-&gt;&gt;P: 父进程退出完成
</code></pre> <p>守护进程有两种常见语境，需要区分：</p> <ul> <li>操作系统中的守护进程：长期运行在后台的服务进程，例如 <code class="language-plaintext highlighter-rouge">sshd</code>、<code class="language-plaintext highlighter-rouge">cron</code>、<code class="language-plaintext highlighter-rouge">nginx</code>。</li> <li>Python <code class="language-plaintext highlighter-rouge">multiprocessing</code> 中的守护子进程：依附于父进程生命周期的子进程。</li> </ul> <p>在 Python 的 <code class="language-plaintext highlighter-rouge">multiprocessing</code> 中，守护子进程通常通过 <code class="language-plaintext highlighter-rouge">daemon=True</code> 设置。它的特点是：父进程退出时，守护子进程通常会被终止，不适合执行必须完整结束的任务。</p> <p>因此：</p> <ul> <li>需要子进程独立完成任务：不要设置为守护子进程。</li> <li>子进程只是辅助父进程工作：可以考虑设置为守护子进程。</li> </ul>]]></content><author><name></name></author><category term="Python"/><category term="Python"/><category term="Process"/><summary type="html"><![CDATA[父进程和子进程的创建、执行、等待与退出关系。]]></summary></entry><entry><title type="html">编写 AI Skill 的随笔</title><link href="https://quaner-dev.github.io/blog/2026/%E7%BC%96%E5%86%99AI-Skill%E7%9A%84%E7%BB%8F%E9%AA%8C/" rel="alternate" type="text/html" title="编写 AI Skill 的随笔"/><published>2026-07-01T00:00:00+00:00</published><updated>2026-07-01T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2026/%E7%BC%96%E5%86%99AI-Skill%E7%9A%84%E7%BB%8F%E9%AA%8C</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2026/%E7%BC%96%E5%86%99AI-Skill%E7%9A%84%E7%BB%8F%E9%AA%8C/"><![CDATA[<h2 id="2026-07-05">2026-07-05</h2> <h3 id="问题和尝试">问题和尝试</h3> <p>编写 AI Skill 时，我一开始的做法是输入需求，让 AI 生成 Skill，再跑端到端测试。流程看起来省事，但结果并不稳定。</p> <p>主要问题出在脚本匹配。脚本执行快，也容易复现结果，但前提是规则覆盖得足够完整。规则一旦漏掉某些情况，名称识别就会出现比较奇怪的结果。有些错误模型能看出来，脚本自己却判断不出来。</p> <p>后来我试过直接让 LLM 识别：先写好规则和筛选条件，再调用 completion 接口，让模型根据上下文判断。小范围数据效果不错，尤其是名称识别、模糊匹配这类问题。但每条数据都调用一次模型，速度太慢，数据量到上万时基本不可用。</p> <h3 id="当前方案">当前方案</h3> <p>目前先改成“脚本匹配 + 模型审核”：</p> <ol> <li>先用脚本处理规则明确、覆盖稳定的数据。</li> <li>再用模型对脚本匹配结果进行审核。</li> <li>如果发现结果不正确，再进入修正流程。</li> <li>如果出现异常数据，说明当前脚本规则覆盖不够，再调用 LLM 进行识别。</li> </ol> <p>这样脚本先处理大部分稳定场景，LLM 只处理不确定或异常的数据。比起全部交给 LLM，这个方式更实际一些。</p> <h3 id="当前阶段的理解">当前阶段的理解</h3> <p>现在的感觉是，AI Skill 不能简单地把所有事情都交给 LLM。脚本适合做批量、稳定、可复现的事情；LLM 更适合做判断、审核和异常兜底。</p> <p>评分和反思也不适合每条都跑模型，最好只放在异常流程里。异常数据本身就说明脚本规则可能没覆盖到，后面需要继续想清楚：哪些异常要交给 LLM，LLM 的审核结果又怎么反过来补充脚本规则。</p>]]></content><author><name></name></author><category term="AI"/><category term="AI-Skill"/><summary type="html"><![CDATA[记录编写 AI Skill 过程中的思考、尝试和阶段性理解。]]></summary></entry><entry><title type="html">Python 信号量介绍</title><link href="https://quaner-dev.github.io/blog/2026/Python%E4%BF%A1%E5%8F%B7%E9%87%8F%E4%BB%8B%E7%BB%8D/" rel="alternate" type="text/html" title="Python 信号量介绍"/><published>2026-07-01T00:00:00+00:00</published><updated>2026-07-01T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2026/Python%E4%BF%A1%E5%8F%B7%E9%87%8F%E4%BB%8B%E7%BB%8D</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2026/Python%E4%BF%A1%E5%8F%B7%E9%87%8F%E4%BB%8B%E7%BB%8D/"><![CDATA[<h2 id="什么是信号量">什么是信号量</h2> <p>信号量（Semaphore）是一种用于控制并发数量的同步工具。</p> <p>可以把它理解成“有限数量的通行证”：</p> <ul> <li>有通行证时，任务可以继续执行。</li> <li>没有通行证时，任务需要等待。</li> <li>任务执行完后，需要归还通行证。</li> </ul> <p>在 Python 中，信号量常用于限制多个线程或协程同时进入某段关键逻辑。</p> <h2 id="基本原理">基本原理</h2> <p>信号量内部维护一个计数器。</p> <ul> <li><code class="language-plaintext highlighter-rouge">acquire()</code>：申请一个资源，计数器减 1。</li> <li><code class="language-plaintext highlighter-rouge">release()</code>：释放一个资源，计数器加 1。</li> <li>当计数器为 0 时，新的任务会阻塞等待。</li> </ul> <pre><code class="language-mermaid">sequenceDiagram
    participant A as 任务A
    participant S as 信号量
    participant B as 任务B

    Note over S: 初始计数 = 1
    A-&gt;&gt;S: acquire()
    S--&gt;&gt;A: 获取成功，计数变为 0
    A-&gt;&gt;A: 访问共享资源

    B-&gt;&gt;S: acquire()
    S--&gt;&gt;B: 计数为 0，进入等待

    A-&gt;&gt;S: release()
    S--&gt;&gt;B: 唤醒等待任务，计数仍由任务B占用
    B-&gt;&gt;B: 访问共享资源
    B-&gt;&gt;S: release()
    Note over S: 计数恢复为 1
</code></pre> <h2 id="状态变化">状态变化</h2> <p>信号量的状态主要由两个部分组成：</p> <ul> <li>当前可用计数。</li> <li>正在等待的任务队列。</li> </ul> <p>当任务调用 <code class="language-plaintext highlighter-rouge">acquire()</code> 时：</p> <ul> <li>如果计数大于 0，说明还有可用资源，任务可以继续执行，计数减 1。</li> <li>如果计数等于 0，说明资源已经被占满，任务会进入等待状态。</li> </ul> <p>当任务调用 <code class="language-plaintext highlighter-rouge">release()</code> 时：</p> <ul> <li>如果有任务正在等待，信号量会唤醒其中一个等待任务。</li> <li>如果没有任务等待，计数加 1，表示多了一个可用资源。</li> </ul> <p>所以，信号量并不是直接管理资源本身，而是通过计数器管理“允许同时进入的任务数量”。</p> <h2 id="机制特点">机制特点</h2> <p>信号量有几个重要特点：</p> <ul> <li>它控制的是并发数量，不是任务执行顺序。</li> <li>它依赖 <code class="language-plaintext highlighter-rouge">acquire()</code> 和 <code class="language-plaintext highlighter-rouge">release()</code> 成对出现。</li> <li>它的初始计数决定了最多允许多少个任务同时进入。</li> <li>它通过等待队列保存暂时无法继续执行的任务。</li> </ul> <h2 id="总结">总结</h2> <p>信号量的本质是一个带等待能力的计数器。</p> <p>它的核心思想很简单：先申请，后执行，最后释放。</p>]]></content><author><name></name></author><category term="Python"/><category term="Python"/><category term="Semaphore"/><summary type="html"><![CDATA[Python 信号量的概念、原理与机制说明。]]></summary></entry><entry><title type="html">OpenClaw官方Docker容器中ClawdHub安装EACCES权限问题记录</title><link href="https://quaner-dev.github.io/blog/2026/OpenClaw%E5%AE%98%E6%96%B9Docker%E5%AE%B9%E5%99%A8%E4%B8%ADClawdHub%E5%AE%89%E8%A3%85EACCES%E6%9D%83%E9%99%90%E9%97%AE%E9%A2%98%E8%AE%B0%E5%BD%95/" rel="alternate" type="text/html" title="OpenClaw官方Docker容器中ClawdHub安装EACCES权限问题记录"/><published>2026-03-23T00:00:00+00:00</published><updated>2026-03-23T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2026/OpenClaw%E5%AE%98%E6%96%B9Docker%E5%AE%B9%E5%99%A8%E4%B8%ADClawdHub%E5%AE%89%E8%A3%85EACCES%E6%9D%83%E9%99%90%E9%97%AE%E9%A2%98%E8%AE%B0%E5%BD%95</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2026/OpenClaw%E5%AE%98%E6%96%B9Docker%E5%AE%B9%E5%99%A8%E4%B8%ADClawdHub%E5%AE%89%E8%A3%85EACCES%E6%9D%83%E9%99%90%E9%97%AE%E9%A2%98%E8%AE%B0%E5%BD%95/"><![CDATA[<h2 id="问题背景">问题背景</h2> <p>最近在使用 OpenClaw 官方 Docker 镜像时，我想通过 OpenClaw 控制台里的技能安装页面安装 ClawdHub，但安装过程直接失败了。表面上看像是一个普通的 npm 安装报错，实际排查下来是一个典型的容器权限问题。</p> <p>这个问题对应的是 GitHub issue <code class="language-plaintext highlighter-rouge">#6620</code>。根据 issue 里的描述，该问题最早在 2026 年 2 月 2 日被报告，触发场景是在官方 Docker 容器中执行 <code class="language-plaintext highlighter-rouge">npm install --global clawdhub</code>。</p> <h2 id="问题现象">问题现象</h2> <p>关键报错如下：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Error: EACCES: permission denied, <span class="nb">mkdir</span> /usr/local/lib/node_modules/clawdhub
</code></pre></div></div> <p>从日志上看，失败点非常明确，<code class="language-plaintext highlighter-rouge">npm install --global clawdhub</code> 在尝试创建全局安装目录时没有权限，最终以退出码 <code class="language-plaintext highlighter-rouge">-13</code> 结束。</p> <p>问题环境大致如下：</p> <ul> <li>官方 OpenClaw Docker 镜像</li> <li>Node <code class="language-plaintext highlighter-rouge">v22.22.0</code></li> <li>npm <code class="language-plaintext highlighter-rouge">10.9.4</code></li> <li>Linux <code class="language-plaintext highlighter-rouge">6.8.12-17-pve</code></li> <li>容器内实际运行用户为 <code class="language-plaintext highlighter-rouge">node</code>，不是 <code class="language-plaintext highlighter-rouge">root</code></li> </ul> <h2 id="根因分析">根因分析</h2> <p>根因其实并不复杂。</p> <p>官方 Dockerfile 在运行阶段使用的是非 root 的 <code class="language-plaintext highlighter-rouge">node</code> 用户，这本身是合理的安全实践。但是 <code class="language-plaintext highlighter-rouge">npm install --global</code> 默认仍然会尝试把包安装到下面这些全局目录：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/usr/local/lib/node_modules
/usr/local/bin
</code></pre></div></div> <p>而这些目录通常是在镜像构建阶段由 <code class="language-plaintext highlighter-rouge">root</code> 创建的，因此目录所有者还是 <code class="language-plaintext highlighter-rouge">root</code>。这样一来，运行中的 <code class="language-plaintext highlighter-rouge">node</code> 用户在执行全局安装时，就会因为没有写权限而触发 <code class="language-plaintext highlighter-rouge">EACCES</code>。</p> <p>也就是说，这个问题并不是 <code class="language-plaintext highlighter-rouge">clawdhub</code> 独有的问题，而是容器里所有依赖 <code class="language-plaintext highlighter-rouge">npm install -g</code> 的安装路径都可能遇到的通用权限问题。</p> <h2 id="临时解决方案">临时解决方案</h2> <p>如果只是想先把功能跑起来，目前我会把临时处理分成两种思路。</p> <h3 id="方案一在运行中的容器里临时修正目录权限">方案一：在运行中的容器里临时修正目录权限</h3> <p>如果容器已经启动，可以先在宿主机执行下面这条命令：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker <span class="nb">exec</span> <span class="nt">-u</span> root openclaw-gateway <span class="nb">chown</span> <span class="nt">-R</span> node:node /usr/local/lib/node_modules /usr/local/bin
</code></pre></div></div> <p>这条命令的作用是进入正在运行的 <code class="language-plaintext highlighter-rouge">openclaw-gateway</code> 容器，以 <code class="language-plaintext highlighter-rouge">root</code> 身份把 npm 全局安装常用目录的所有者改成 <code class="language-plaintext highlighter-rouge">node</code> 用户。这样做之后，再执行全局安装时，权限问题通常就不会再出现。</p> <p>不过这个方式本质上还是运行时修补，容器重建之后是否仍然有效，要看实际部署方式。</p> <h3 id="方案二绕过全局安装改成本地安装">方案二：绕过全局安装，改成本地安装</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /home/node/.openclaw/workspace
npm <span class="nb">install </span>clawdhub
npx clawdhub <span class="nt">--help</span>
</code></pre></div></div> <p>这种方式的思路很直接：</p> <ul> <li>不再写入 <code class="language-plaintext highlighter-rouge">/usr/local/lib/node_modules</code></li> <li>改为安装到当前工作目录下的 <code class="language-plaintext highlighter-rouge">node_modules</code></li> <li>通过 <code class="language-plaintext highlighter-rouge">npx</code> 直接调用本地安装的命令</li> </ul> <p>对于临时验证功能来说，这个方案是够用的。</p> <h2 id="总结">总结</h2> <p>这次问题本质上不是“ClawdHub 安装器坏了”，而是“官方 Docker 容器在非 root 用户下执行 <code class="language-plaintext highlighter-rouge">npm install --global</code> 时，遇到了全局安装目录的权限限制”。</p> <p>在不改动官方 Dockerfile 的前提下，这篇记录的重点就是把现象和根因留档，并给出一个可用的临时绕过方式：避免全局安装，改为在工作目录里本地安装并通过 <code class="language-plaintext highlighter-rouge">npx</code> 调用。</p> <p>如果后续官方镜像修复了这个权限问题，再回头更新这篇记录也会比较清楚。</p>]]></content><author><name></name></author><category term="AI"/><category term="OpenClaw"/><category term="Docker"/><category term="npm"/><category term="ClawdHub"/><summary type="html"><![CDATA[问题背景]]></summary></entry><entry><title type="html">Linux初始化磁盘</title><link href="https://quaner-dev.github.io/blog/2026/Linux%E5%88%9D%E5%A7%8B%E5%8C%96%E7%A3%81%E7%9B%98/" rel="alternate" type="text/html" title="Linux初始化磁盘"/><published>2026-02-11T00:00:00+00:00</published><updated>2026-02-11T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2026/Linux%E5%88%9D%E5%A7%8B%E5%8C%96%E7%A3%81%E7%9B%98</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2026/Linux%E5%88%9D%E5%A7%8B%E5%8C%96%E7%A3%81%E7%9B%98/"><![CDATA[<h2 id="注意事项">注意事项</h2> <ul> <li>分区和格式化会清空目标盘数据，请提前做好快照或备份</li> <li>以下示例默认目标盘为 <code class="language-plaintext highlighter-rouge">/dev/vdb</code>，请先确认盘符，避免误操作系统盘</li> </ul> <h2 id="文件系统类型特点">文件系统类型特点</h2> <table> <thead> <tr> <th>文件系统</th> <th>特点</th> <th>适用场景</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">ext4</code></td> <td>稳定性高、兼容性好、工具链完善</td> <td>大多数通用业务场景，维护成本低</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">xfs</code></td> <td>大文件和高并发写入表现较好，在线扩容能力强，不支持直接缩容</td> <td>日志、归档、大吞吐数据场景</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">btrfs</code></td> <td>支持快照、校验、子卷等高级特性</td> <td>需要快照和高级卷管理能力的场景</td> </tr> </tbody> </table> <p>本文操作步骤采用 <code class="language-plaintext highlighter-rouge">ext4</code>，优先保证通用性与稳定性</p> <h2 id="操作步骤">操作步骤</h2> <h3 id="1-识别目标磁盘">1. 识别目标磁盘</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lsblk <span class="nt">-f</span>
</code></pre></div></div> <p>确认目标磁盘为未使用状态（通常 <code class="language-plaintext highlighter-rouge">FSTYPE</code> 为空）</p> <h3 id="2-创建-gpt-分区单分区占满整盘">2. 创建 GPT 分区（单分区占满整盘）</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>parted /dev/vdb
</code></pre></div></div> <p>示例会话：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(parted) mklabel gpt
(parted) mkpart primary 1MiB 100%
(parted) print
(parted) quit
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>partprobe /dev/vdb
lsblk
</code></pre></div></div> <p>执行后应看到新分区 <code class="language-plaintext highlighter-rouge">/dev/vdb1</code></p> <h3 id="3-创建文件系统">3. 创建文件系统</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>mkfs.ext4 <span class="nt">-F</span> /dev/vdb1
</code></pre></div></div> <p>查看文件系统结果：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lsblk <span class="nt">-f</span>
</code></pre></div></div> <h3 id="4-挂载到目录">4. 挂载到目录</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo mkdir</span> <span class="nt">-p</span> /data
<span class="nb">sudo </span>mount /dev/vdb1 /data
</code></pre></div></div> <p>验证：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lsblk
<span class="nb">df</span> <span class="nt">-h</span> | <span class="nb">grep</span> /data
</code></pre></div></div> <h3 id="5-配置开机自动挂载">5. 配置开机自动挂载</h3> <p>先备份 <code class="language-plaintext highlighter-rouge">fstab</code>：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo cp</span> /etc/fstab /etc/fstab.bak
</code></pre></div></div> <p>获取分区 UUID：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>blkid /dev/vdb1
</code></pre></div></div> <p>编辑 <code class="language-plaintext highlighter-rouge">/etc/fstab</code>，追加一行（ext4 示例）：</p> <pre><code class="language-fstab">UUID=xxxx-xxxx-xxxx-xxxx /data ext4 defaults,nofail 0 2
</code></pre> <h3 id="6-校验挂载配置">6. 校验挂载配置</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>umount /data
<span class="nb">sudo </span>mount <span class="nt">-a</span>
lsblk
<span class="nb">df</span> <span class="nt">-h</span> | <span class="nb">grep</span> /data
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">mount -a</code> 无报错且 <code class="language-plaintext highlighter-rouge">/data</code> 已挂载，即表示配置成功</p> <h2 id="磁盘大于-2tib-的处理">磁盘大于 2TiB 的处理</h2> <p>当磁盘容量大于 <code class="language-plaintext highlighter-rouge">2TiB</code> 时，流程与上文基本一致，但必须确认分区表为 <code class="language-plaintext highlighter-rouge">GPT</code></p> <h3 id="1-检查分区表类型">1. 检查分区表类型</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>parted /dev/vdb
</code></pre></div></div> <p>示例会话：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(parted) print
(parted) quit
</code></pre></div></div> <p>若看到 <code class="language-plaintext highlighter-rouge">Partition Table: msdos</code>，则需改成 <code class="language-plaintext highlighter-rouge">gpt</code>（会清空现有分区数据）</p> <h3 id="2-手动重建为-gpt-分区">2. 手动重建为 GPT 分区</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>parted /dev/vdb
</code></pre></div></div> <p>示例会话：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(parted) mklabel gpt
(parted) mkpart primary 1MiB 100%
(parted) print
(parted) quit
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>partprobe /dev/vdb
</code></pre></div></div> <h3 id="3-格式化和挂载">3. 格式化和挂载</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>mkfs.ext4 <span class="nt">-F</span> /dev/vdb1
<span class="nb">sudo mkdir</span> <span class="nt">-p</span> /data
<span class="nb">sudo </span>mount /dev/vdb1 /data
</code></pre></div></div> <h3 id="4-扩容后超过-2tib-的情况">4. 扩容后超过 2TiB 的情况</h3> <p>如果是磁盘扩容后超过 <code class="language-plaintext highlighter-rouge">2TiB</code>，通常还需要扩展分区与文件系统：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>growpart /dev/vdb 1
<span class="nb">sudo </span>resize2fs /dev/vdb1
lsblk
<span class="nb">df</span> <span class="nt">-h</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="Linux"/><category term="Disk"/><summary type="html"><![CDATA[注意事项]]></summary></entry><entry><title type="html">TimeShift备份系统</title><link href="https://quaner-dev.github.io/blog/2026/TimeShift%E5%A4%87%E4%BB%BD%E7%B3%BB%E7%BB%9F/" rel="alternate" type="text/html" title="TimeShift备份系统"/><published>2026-02-11T00:00:00+00:00</published><updated>2026-02-11T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2026/TimeShift%E5%A4%87%E4%BB%BD%E7%B3%BB%E7%BB%9F</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2026/TimeShift%E5%A4%87%E4%BB%BD%E7%B3%BB%E7%BB%9F/"><![CDATA[<h2 id="timeshift简介">TimeShift简介</h2> <p>TimeShift 是 Linux 上常用的系统快照工具，适合做系统层面的备份与还原。它更像 Windows 的系统还原点：主要保护系统文件、软件配置与关键目录，而不是用来做个人文 件的长期备份。默认情况下，用户目录通常会被排除在快照之外，你可以按需调整。</p> <p>适用场景：</p> <ul> <li>升级系统或安装驱动前做一次系统快照</li> <li>系统配置改动较大时做阶段性快照</li> <li>出现故障时快速回滚到可用状态</li> </ul> <h2 id="操作文档">操作文档</h2> <h3 id="安装-timeshift">安装 Timeshift</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt update
<span class="nb">sudo </span>apt <span class="nb">install </span>timeshift
</code></pre></div></div> <p>也可以从应用菜单直接打开，或在终端执行：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>timeshift-gtk
</code></pre></div></div> <h3 id="首次启动配置向导">首次启动配置向导</h3> <p>首次打开会进入向导，按提示完成以下步骤。</p> <ol> <li> <p>选择快照类型（RSYNC 或 BTRFS）</p> <p><img src="/assets/img/post_img/2026-02-11-TimeShift备份系统/1.png" alt="选择快照类型"/></p> <ul> <li><strong>RSYNC</strong>：适用于绝大多数非 Btrfs 文件系统，通用性最好</li> <li><strong>BTRFS</strong>：仅适用于 Btrfs 文件系统，创建和回滚速度更快</li> </ul> </li> <li> <p>选择快照存储位置</p> <p><img src="/assets/img/post_img/2026-02-11-TimeShift备份系统/2.png" alt="选择快照存储位置"/></p> <p>建议放在独立分区或外接硬盘，避免系统盘损坏时快照不可用。</p> </li> <li> <p>设置快照计划任务</p> <p><img src="/assets/img/post_img/2026-02-11-TimeShift备份系统/3.png" alt="设置快照计划任务"/></p> <p>可按“每小时 / 每日 / 每周 / 每月”设定频率。个人桌面环境通常使用“每日 + 每周”即可。</p> </li> </ol> <h3 id="浏览与恢复快照">浏览与恢复快照</h3> <p>选中目标快照，点击“浏览”可查看变更内容；点击“恢复”可进行系统回滚。</p> <p><img src="/assets/img/post_img/2026-02-11-TimeShift备份系统/4.png" alt="浏览与恢复快照"/></p> <p>恢复前建议：</p> <ul> <li>关闭正在运行的重要应用</li> <li>确认快照来自系统稳定状态</li> <li>如果是关键系统盘，建议先备份用户重要数据</li> </ul> <h3 id="清理与维护">清理与维护</h3> <p>定期清理旧快照可以释放空间，也能降低恢复时的选择成本。</p> <p><img src="/assets/img/post_img/2026-02-11-TimeShift备份系统/4.png" alt="清理旧快照"/></p> <h3 id="恢复完成后的检查">恢复完成后的检查</h3> <p>恢复完成后可重启系统，并检查以下内容：</p> <ul> <li>系统服务是否正常启动</li> <li>关键软件是否可用</li> <li>网络与驱动是否正常</li> </ul>]]></content><author><name></name></author><category term="Backup"/><category term="Timeshift"/><summary type="html"><![CDATA[TimeShift简介]]></summary></entry><entry><title type="html">Ubuntu开启RDP</title><link href="https://quaner-dev.github.io/blog/2026/Ubuntu%E5%BC%80%E5%90%AFRDP/" rel="alternate" type="text/html" title="Ubuntu开启RDP"/><published>2026-02-11T00:00:00+00:00</published><updated>2026-02-11T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2026/Ubuntu%E5%BC%80%E5%90%AFRDP</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2026/Ubuntu%E5%BC%80%E5%90%AFRDP/"><![CDATA[<h2 id="ubuntu-rdp简介">Ubuntu RDP简介</h2> <p>RDP 是远程桌面协议，可让你在其他设备上图形化登录 Ubuntu 桌面。本文基于 Ubuntu 自带的远程登录功能进行配置，适合局域网或可信网络使用。</p> <h2 id="rdp配置">RDP配置</h2> <p>进入 <code class="language-plaintext highlighter-rouge">Settings</code> -&gt; <code class="language-plaintext highlighter-rouge">System</code> -&gt; <code class="language-plaintext highlighter-rouge">Remote Desktop</code>，开启远程桌面入口。</p> <p><img src="/assets/img/post_img/2026-02-11-Ubuntu开启RDP/1.png" alt="RDP选项"/></p> <h2 id="remote-login配置">Remote Login配置</h2> <p>在 Remote Login 页面完成以下步骤。</p> <ol> <li> <p>选择 Remote Login 选项。</p> <p><img src="/assets/img/post_img/2026-02-11-Ubuntu开启RDP/2.png" alt="Desktop Sharing"/></p> </li> <li> <p>点击 Unlock，输入密码解锁配置权限。</p> <p><img src="/assets/img/post_img/2026-02-11-Ubuntu开启RDP/3.png" alt="Desktop Sharing"/></p> </li> <li> <p>开启 Remote Login，并在 Login Details 中设置密码。</p> <p><img src="/assets/img/post_img/2026-02-11-Ubuntu开启RDP/4.png" alt="Desktop Sharing"/></p> </li> </ol> <h2 id="连接说明">连接说明</h2> <p>在远程设备上使用 RDP 客户端连接 Ubuntu 的 IP 地址，账号为 Ubuntu 用户名， 密码为上一步设置的 Remote Login 密码。</p>]]></content><author><name></name></author><category term="Ubuntu"/><category term="RDP"/><summary type="html"><![CDATA[Ubuntu RDP简介]]></summary></entry></feed>