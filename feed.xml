<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="cn"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://quaner-dev.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://quaner-dev.github.io/" rel="alternate" type="text/html" hreflang="cn"/><updated>2024-06-22T14:09:32+00:00</updated><id>https://quaner-dev.github.io/feed.xml</id><title type="html">blank</title><subtitle>A technical blog about daily summaries. </subtitle><entry><title type="html">K8S笔记</title><link href="https://quaner-dev.github.io/blog/2024/K8S%E7%AC%94%E8%AE%B0/" rel="alternate" type="text/html" title="K8S笔记"/><published>2024-05-14T00:00:00+00:00</published><updated>2024-05-14T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2024/K8S%E7%AC%94%E8%AE%B0</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2024/K8S%E7%AC%94%E8%AE%B0/"><![CDATA[<h1 id="k8s-笔记">K8S 笔记</h1> <h2 id="第一章-kubernetes-基础">第一章 Kubernetes 基础</h2> <p>Kubernetes 是谷歌以 Borg 为前身，基于谷歌 15 年生产环境经验的基础上开源的一个项目，Kubernetes 致力于提供跨主机集群的自动部署、扩展、高可用以及运行应用程序容器的平台。</p> <h3 id="11-master-节点整个集群的控制中枢">1.1 Master 节点：整个集群的控制中枢</h3> <ul> <li>Kube-APIServer：集群的控制中枢，各个模块之间信息交互都需要经过 Kube-APIServer，同时它也是集群管理、资源配置、整个集群安全机制的入口。</li> <li>Controller-Manager：集群的状态管理器，保证 Pod 或其他资源达到期望值，也是需要和 APIServer 进行通信，在需要的时候创建、更新或删除它所管理的资源。</li> <li>Scheduler：集群的调度中心，它会根据指定的一系列条件，选择一个或一批最佳的节点，然后部署我们的 Pod。</li> <li>Etcd：键值数据库，报错一些集群的信息，一般生产环境中建议部署三个以上节点（奇数个）。</li> </ul> <h3 id="12-node工作节点">1.2 Node：工作节点</h3> <h4 id="workernode-节点minion-节点">Worker、node 节点、minion 节点</h4> <ul> <li>Kubelet：负责监听节点上 Pod 的状态，同时负责上报节点和节点上面 Pod 的状态，负责与 Master 节点通信，并管理节点上面的 Pod。</li> <li>Kube-proxy：负责 Pod 之间的通信和负载均衡，将指定的流量分发到后端正确的机器上。</li> <li>查看 Kube-proxy 工作模式：curl 127.0.0.1:10249/proxyMode <ul> <li>Ipvs：监听 Master 节点增加和删除 service 以及 endpoint 的消息，调用 Netlink 接口创建相应的 IPVS 规则。通过 IPVS 规则，将流量转发至相应的 Pod 上。</li> <li>Iptables：监听 Master 节点增加和删除 service 以及 endpoint 的消息，对于每一个 Service，他都会场景一个 iptables 规则，将 service 的 clusterIP 代理到后端对应的 Pod。</li> </ul> </li> </ul> <h4 id="其他组件">其他组件</h4> <ul> <li>Calico：符合 CNI 标准的网络插件，给每个 Pod 生成一个唯一的 IP 地址，并且把每个节点当做一个路由器。Cilium</li> <li>CoreDNS：用于 Kubernetes 集群内部 Service 的解析，可以让 Pod 把 Service 名称解析成 IP 地址，然后通过 Service 的 IP 地址进行连接到对应的应用上。</li> <li>Docker：容器引擎，负责对容器的管理。</li> </ul> <h2 id="第二章-pod">第二章 Pod</h2> <h3 id="11-什么是-pod">1.1 什么是 Pod</h3> <p>Pod 是 Kubernetes 中最小的单元，它由一组、一个或多个容器组成，每个 Pod 还包含了一个 Pause 容器，Pause 容器是 Pod 的父容器，主要负责僵尸进程的回收管理，通过 Pause 容器可以使同一个 Pod 里面的多个容器共享存储、网络、PID、IPC 等。</p> <h3 id="12-定义一个-pod">1.2 定义一个 Pod</h3> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span> <span class="c1"># 必选，API的版本号</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span> <span class="c1"># 必选，类型Pod</span>
<span class="na">metadata</span><span class="pi">:</span> <span class="c1"># 必选，元数据</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span> <span class="c1"># 必选，符合RFC 1035规范的Pod名称</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">default</span> <span class="c1"># 可选，Pod所在的命名空间，不指定默认为default，可以使用 -n 指定namespace</span>
  <span class="na">labels</span><span class="pi">:</span> <span class="c1"># 可选，标签选择器，一般用于过滤和区分Pod</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">nginx</span>
    <span class="na">role</span><span class="pi">:</span> <span class="s">frontend</span> <span class="c1"># 可以写多个</span>
  <span class="na">annotations</span><span class="pi">:</span> <span class="c1"># 可选，注释列表，可以写多个</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">nginx</span>
<span class="na">spec</span><span class="pi">:</span> <span class="c1"># 必选，用于定义容器的详细信息</span>
  <span class="na">initContainers</span><span class="pi">:</span> <span class="c1"># 初始化容器，在容器启动之前执行的一些初始化操作</span>
    <span class="pi">-</span> <span class="na">command</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="s">sh</span>
        <span class="pi">-</span> <span class="s">-c</span>
        <span class="pi">-</span> <span class="s">echo "I am InitContainer for init some configuration"</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">busybox</span>
      <span class="na">imagePullPolicy</span><span class="pi">:</span> <span class="s">IfNotPresent</span>
      <span class="na">name</span><span class="pi">:</span> <span class="s">init-container</span>
  <span class="na">containers</span><span class="pi">:</span> <span class="c1"># 必选，容器列表</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span> <span class="c1"># 必选，符合RFC 1035规范的容器名称</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx:latest</span> <span class="c1"># 必选，容器所用的镜像的地址</span>
      <span class="na">imagePullPolicy</span><span class="pi">:</span> <span class="s">Always</span> <span class="c1"># 可选，镜像拉取策略</span>
      <span class="na">command</span><span class="pi">:</span> <span class="c1"># 可选，容器启动执行的命令</span>
        <span class="pi">-</span> <span class="s">nginx</span>
        <span class="pi">-</span> <span class="s">-g</span>
        <span class="pi">-</span> <span class="s2">"</span><span class="s">daemon</span><span class="nv"> </span><span class="s">off;"</span>
      <span class="na">workingDir</span><span class="pi">:</span> <span class="s">/usr/share/nginx/html</span> <span class="c1"># 可选，容器的工作目录</span>
      <span class="na">volumeMounts</span><span class="pi">:</span> <span class="c1"># 可选，存储卷配置，可以配置多个</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">webroot</span> <span class="c1"># 存储卷名称</span>
          <span class="na">mountPath</span><span class="pi">:</span> <span class="s">/usr/share/nginx/html</span> <span class="c1"># 挂载目录</span>
          <span class="na">readOnly</span><span class="pi">:</span> <span class="kc">true</span> <span class="c1"># 只读</span>
      <span class="na">ports</span><span class="pi">:</span> <span class="c1"># 可选，容器需要暴露的端口号列表</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http</span> <span class="c1"># 端口名称</span>
          <span class="na">containerPort</span><span class="pi">:</span> <span class="m">80</span> <span class="c1"># 端口号</span>
          <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span> <span class="c1"># 端口协议，默认TCP</span>
      <span class="na">env</span><span class="pi">:</span> <span class="c1"># 可选，环境变量配置列表</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">TZ</span> <span class="c1"># 变量名</span>
          <span class="na">value</span><span class="pi">:</span> <span class="s">Asia/Shanghai</span> <span class="c1"># 变量的值</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">LANG</span>
          <span class="na">value</span><span class="pi">:</span> <span class="s">en_US.utf8</span>
      <span class="na">resources</span><span class="pi">:</span> <span class="c1"># 可选，资源限制和资源请求限制</span>
        <span class="na">limits</span><span class="pi">:</span> <span class="c1"># 最大限制设置</span>
          <span class="na">cpu</span><span class="pi">:</span> <span class="s">1000m</span>
          <span class="na">memory</span><span class="pi">:</span> <span class="s">1024Mi</span>
        <span class="na">requests</span><span class="pi">:</span> <span class="c1"># 启动所需的资源</span>
          <span class="na">cpu</span><span class="pi">:</span> <span class="s">100m</span>
          <span class="na">memory</span><span class="pi">:</span> <span class="s">512Mi</span>
      <span class="na">startupProbe</span><span class="pi">:</span> <span class="c1"># 可选，检测容器内进程是否完成启动。注意三种检查方式同时只能使用一种。</span>
        <span class="na">httpGet</span><span class="pi">:</span> <span class="c1"># httpGet检测方式，生产环境建议使用httpGet实现接口级健康检查，健康检查由应用程序提供。</span>
          <span class="na">path</span><span class="pi">:</span> <span class="s">/api/successStart</span> <span class="c1"># 检查路径</span>
          <span class="na">port</span><span class="pi">:</span> <span class="m">80</span>
      <span class="na">readinessProbe</span><span class="pi">:</span> <span class="c1"># 可选，健康检查。注意三种检查方式同时只能使用一种。</span>
        <span class="na">httpGet</span><span class="pi">:</span> <span class="c1"># httpGet检测方式，生产环境建议使用httpGet实现接口级健康检查，健康检查由应用程序提供。</span>
          <span class="na">path</span><span class="pi">:</span> <span class="s">/</span> <span class="c1"># 检查路径</span>
          <span class="na">port</span><span class="pi">:</span> <span class="m">80</span> <span class="c1"># 监控端口</span>
      <span class="na">livenessProbe</span><span class="pi">:</span> <span class="c1"># 可选，健康检查</span>
        <span class="na">exec</span><span class="pi">:</span> <span class="c1"># 执行容器命令检测方式</span>
        <span class="na">command</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="s">cat</span>
          <span class="pi">-</span> <span class="s">/health</span>
        <span class="na">httpGet</span><span class="pi">:</span> <span class="c1"># httpGet检测方式</span>
          <span class="na">path</span><span class="pi">:</span> <span class="s">/_health</span> <span class="c1"># 检查路径</span>
          <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>
          <span class="na">httpHeaders</span><span class="pi">:</span> <span class="c1"># 检查的请求头</span>
            <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">end-user</span>
              <span class="na">value</span><span class="pi">:</span> <span class="s">Jason</span>
        <span class="na">tcpSocket</span><span class="pi">:</span> <span class="c1"># 端口检测方式</span>
          <span class="na">port</span><span class="pi">:</span> <span class="m">80</span>
        <span class="na">initialDelaySeconds</span><span class="pi">:</span> <span class="m">60</span> <span class="c1"># 初始化时间</span>
        <span class="na">timeoutSeconds</span><span class="pi">:</span> <span class="m">2</span> <span class="c1"># 超时时间</span>
        <span class="na">periodSeconds</span><span class="pi">:</span> <span class="m">5</span> <span class="c1"># 检测间隔</span>
        <span class="na">successThreshold</span><span class="pi">:</span> <span class="m">1</span> <span class="c1"># 检查成功为2次表示就绪</span>
        <span class="na">failureThreshold</span><span class="pi">:</span> <span class="m">2</span> <span class="c1"># 检测失败1次表示未就绪</span>
      <span class="na">lifecycle</span><span class="pi">:</span>
        <span class="na">postStart</span><span class="pi">:</span> <span class="c1"># 容器创建完成后执行的指令, 可以是exec httpGet TCPSocket</span>
          <span class="na">exec</span><span class="pi">:</span>
            <span class="na">command</span><span class="pi">:</span>
              <span class="pi">-</span> <span class="s">sh</span>
              <span class="pi">-</span> <span class="s">-c</span>
              <span class="pi">-</span> <span class="s2">"</span><span class="s">mkdir</span><span class="nv"> </span><span class="s">/data/</span><span class="nv"> </span><span class="s">"</span>
        <span class="na">preStop</span><span class="pi">:</span>
          <span class="na">httpGet</span><span class="pi">:</span>
            <span class="na">path</span><span class="pi">:</span> <span class="s">/</span>
            <span class="na">port</span><span class="pi">:</span> <span class="m">80</span>
          <span class="na">exec</span><span class="pi">:</span>
            <span class="na">command</span><span class="pi">:</span>
              <span class="pi">-</span> <span class="s">sh</span>
              <span class="pi">-</span> <span class="s">-c</span>
              <span class="pi">-</span> <span class="s">sleep </span><span class="m">9</span>
  <span class="na">restartPolicy</span><span class="pi">:</span> <span class="s">Always</span> <span class="c1"># 可选，默认为Always</span>
  <span class="na">nodeSelector</span><span class="pi">:</span> <span class="c1"># 可选，指定Node节点</span>
    <span class="na">region</span><span class="pi">:</span> <span class="s">subnet7</span>
  <span class="na">imagePullSecrets</span><span class="pi">:</span> <span class="c1"># 可选，拉取镜像使用的secret，可以配置多个</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">default-dockercfg-86258</span>
  <span class="na">hostNetwork</span><span class="pi">:</span> <span class="kc">false</span> <span class="c1"># 可选，是否为主机模式，如是，会占用主机端口</span>
  <span class="na">volumes</span><span class="pi">:</span> <span class="c1"># 共享存储卷列表</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">webroot</span> <span class="c1"># 名称，与上述对应</span>
      <span class="na">emptyDir</span><span class="pi">:</span> <span class="pi">{}</span> <span class="c1"># 挂载目录</span>
      <span class="na">hostPath</span><span class="pi">:</span> <span class="c1"># 挂载本机目录</span>
        <span class="na">path</span><span class="pi">:</span> <span class="s">/etc/hosts</span>
</code></pre></div></div> <h3 id="13-pod-探针">1.3 Pod 探针</h3> <ul> <li>StartupProbe：k8s1.16 版本后新加的探测方式，用于判断容器内应用程序是否已经启动。如果配置了 startupProbe，就会先禁止其他的探测，直到它成功为止，成功后将不在进行探测。</li> <li>LivenessProbe：用于探测容器是否运行，如果探测失败，kubelet 会根据配置的重启策略进行相应的处理。若没有配置该探针，默认就是 success。</li> <li>ReadinessProbe：一般用于探测容器内的程序是否健康，它的返回值如果为 success，那么久代表这个容器已经完成启动，并且程序已经是可以接受流量的状态。</li> </ul> <h3 id="14-pod-探针的检测方式">1.4 Pod 探针的检测方式</h3> <ul> <li>ExecAction：在容器内执行一个命令，如果返回值为 0，则认为容器健康。</li> <li>TCPSocketAction：通过 TCP 连接检查容器内的端口是否是通的，如果是通的就认为容器健康。</li> <li>HTTPGetAction：通过应用程序暴露的 API 地址来检查程序是否是正常的，如果状态码为 200~400 之间，则认为容器健康。</li> </ul> <h3 id="15-探针检查参数配置">1.5 探针检查参数配置</h3> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">initialDelaySeconds</span><span class="pi">:</span> <span class="m">60</span> <span class="c1"># 初始化时间</span>
<span class="na">timeoutSeconds</span><span class="pi">:</span> <span class="m">2</span> <span class="c1"># 超时时间</span>
<span class="na">periodSeconds</span><span class="pi">:</span> <span class="m">5</span> <span class="c1"># 检测间隔</span>
<span class="na">successThreshold</span><span class="pi">:</span> <span class="m">1</span> <span class="c1"># 检查成功为1次表示就绪</span>
<span class="na">failureThreshold</span><span class="pi">:</span> <span class="m">2</span> <span class="c1"># 检测失败2次表示未就绪</span>
</code></pre></div></div> <p>Prestop：先去请求 eureka 接口，把自己的 IP 地址和端口号，进行下线，eureka 从注册表中删除该应用的 IP 地址。然后容器进行 sleep 90；kill <code class="language-plaintext highlighter-rouge">pgrep java</code></p> <h2 id="第三章-rcrs">第三章 RC&amp;RS</h2> <h3 id="11-replication-controller-和-replicaset">1.1 Replication Controller 和 ReplicaSet</h3> <p>Replication Controller（复制控制器，RC）和 ReplicaSet（复制集，RS）是两种简单部署 Pod 的方式。因为在生产环境中，主要使用更高级的 Deployment 等方式进行 Pod 的管理和部署，所以本节只对 Replication Controller 和 Replica Set 的部署方式进行简单介绍。</p> <h4 id="111-replication-controller">1.1.1 Replication Controller</h4> <p>Replication Controller（简称 RC）可确保 Pod 副本数达到期望值，也就是 RC 定义的数量。换句话说，Replication Controller 可确保一个 Pod 或一组同类 Pod 总是可用。 如果存在的 Pod 大于设定的值，则 Replication Controller 将终止额外的 Pod。如果太小，Replication Controller 将启动更多的 Pod 用于保证达到期望值。与手动创建 Pod 不同的是，用 Replication Controller 维护的 Pod 在失败、删除或终止时会自动替换。因此即使应用程序只需要一个 Pod，也应该使用 Replication Controller 或其他方式管理。Replication Controller 类似于进程管理程序，但是 Replication Controller 不是监视单个节点上的各个进程，而是监视多个节点上的多个 Pod。 定义一个 Replication Controller 的示例如下。</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">ReplicationController</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">nginx</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">nginx</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
          <span class="na">ports</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">80</span>
</code></pre></div></div> <h4 id="112-replicaset">1.1.2 ReplicaSet</h4> <p>ReplicaSet 是支持基于集合的标签选择器的下一代 Replication Controller，它主要用作 Deployment 协调创建、删除和更新 Pod，和 Replication Controller 唯一的区别是，ReplicaSet 支持标签选择器。在实际应用中，虽然 ReplicaSet 可以单独使用，但是一般建议使用 Deployment 来自动管理 ReplicaSet，除非自定义的 Pod 不需要更新或有其他编排等。 定义一个 ReplicaSet 的示例如下：</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">ReplicaSet</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">frontend</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">guestbook</span>
    <span class="na">tier</span><span class="pi">:</span> <span class="s">frontend</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="c1"># modify replicas according to your case</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">tier</span><span class="pi">:</span> <span class="s">frontend</span>
    <span class="na">matchExpressions</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="pi">{</span> <span class="nv">key</span><span class="pi">:</span> <span class="nv">tier</span><span class="pi">,</span> <span class="nv">operator</span><span class="pi">:</span> <span class="nv">In</span><span class="pi">,</span> <span class="nv">values</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">frontend</span><span class="pi">]</span> <span class="pi">}</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">guestbook</span>
        <span class="na">tier</span><span class="pi">:</span> <span class="s">frontend</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">php-redis</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">gcr.io/google_samples/gb-frontend:v3</span>
          <span class="na">resources</span><span class="pi">:</span>
            <span class="na">requests</span><span class="pi">:</span>
              <span class="na">cpu</span><span class="pi">:</span> <span class="s">100m</span>
              <span class="na">memory</span><span class="pi">:</span> <span class="s">100Mi</span>
          <span class="na">env</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">GET_HOSTS_FROM</span>
              <span class="na">value</span><span class="pi">:</span> <span class="s">dns</span>
              <span class="c1"># If your cluster config does not include a dns service, then to</span>
              <span class="c1"># instead access environment variables to find service host</span>
              <span class="c1"># info, comment out the 'value: dns' line above, and uncomment the</span>
              <span class="c1"># line below.</span>
              <span class="c1"># value: env</span>
          <span class="na">ports</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">80</span>
</code></pre></div></div> <p>Replication Controller 和 ReplicaSet 的创建删除和 Pod 并无太大区别，Replication Controller 目前几乎已经不在生产环境中使用，ReplicaSet 也很少单独被使用，都是使用更高级的资源 Deployment、DaemonSet、StatefulSet 进行管理 Pod。</p> <h2 id="第四章-deployment">第四章 Deployment</h2> <h3 id="11-deployment-概念">1.1 Deployment 概念</h3> <p>用于部署无状态的服务，这个最常用的控制器。一般用于管理维护企业内部无状态的微服务，比如 configserver、zuul、springboot。他可以管理多个副本的 Pod 实现无缝迁移、自动扩容缩容、自动灾难恢复、一键回滚等功能。</p> <h3 id="12-创建一个-deployment">1.2 创建一个 Deployment</h3> <p>手动创建：kubectl create deployment nginx –image=nginx:1.15.2 从文件创建：</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">annotations</span><span class="pi">:</span>
    <span class="na">deployment.kubernetes.io/revision</span><span class="pi">:</span> <span class="s2">"</span><span class="s">1"</span>
  <span class="na">creationTimestamp</span><span class="pi">:</span> <span class="s2">"</span><span class="s">2020-09-19T02:41:11Z"</span>
  <span class="na">generation</span><span class="pi">:</span> <span class="m">1</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">nginx</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">default</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">progressDeadlineSeconds</span><span class="pi">:</span> <span class="m">600</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">2</span> <span class="c1"># 副本数</span>
  <span class="na">revisionHistoryLimit</span><span class="pi">:</span> <span class="m">10</span> <span class="c1"># 历史记录保留的个数</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">nginx</span>
  <span class="na">strategy</span><span class="pi">:</span>
    <span class="na">rollingUpdate</span><span class="pi">:</span>
      <span class="na">maxSurge</span><span class="pi">:</span> <span class="s">25%</span>
      <span class="na">maxUnavailable</span><span class="pi">:</span> <span class="s">25%</span>
    <span class="na">type</span><span class="pi">:</span> <span class="s">RollingUpdate</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">creationTimestamp</span><span class="pi">:</span> <span class="kc">null</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">nginx</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">image</span><span class="pi">:</span> <span class="s">nginx:1.15.2</span>
          <span class="na">imagePullPolicy</span><span class="pi">:</span> <span class="s">IfNotPresent</span>
          <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
          <span class="na">resources</span><span class="pi">:</span> <span class="pi">{}</span>
          <span class="na">terminationMessagePath</span><span class="pi">:</span> <span class="s">/dev/termination-log</span>
          <span class="na">terminationMessagePolicy</span><span class="pi">:</span> <span class="s">File</span>
      <span class="na">dnsPolicy</span><span class="pi">:</span> <span class="s">ClusterFirst</span>
      <span class="na">restartPolicy</span><span class="pi">:</span> <span class="s">Always</span>
      <span class="na">schedulerName</span><span class="pi">:</span> <span class="s">default-scheduler</span>
      <span class="na">securityContext</span><span class="pi">:</span> <span class="pi">{}</span>
      <span class="na">terminationGracePeriodSeconds</span><span class="pi">:</span> <span class="m">30</span>
</code></pre></div></div> <p>状态解析：</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get deploy <span class="nt">-owide</span>
</code></pre></div></div> <ul> <li>NAME： Deployment 名称</li> <li>READY：Pod 的状态，已经 Ready 的个数</li> <li>UP-TO-DATE：已经达到期望状态的被更新的副本数</li> <li>AVAILABLE：已经可以用的副本数</li> <li>AGE：显示应用程序运行的时间</li> <li>CONTAINERS：容器名称</li> <li>IMAGES：容器的镜像</li> <li>SELECTOR：管理的 Pod 的标签</li> </ul> <h3 id="13-deployment-的更新">1.3 Deployment 的更新</h3> <p>更改 deployment 的镜像并记录：</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">set </span>image deploy nginx <span class="nv">nginx</span><span class="o">=</span>nginx:1.15.3 –record
</code></pre></div></div> <p>查看更新过程：</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout status deploy nginx
</code></pre></div></div> <p>或者使用 describe 查看：</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe deploy nginx
</code></pre></div></div> <h3 id="14-deployment-回滚">1.4 Deployment 回滚</h3> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 执行更新操作</span>
kubectl <span class="nb">set </span>image deploy nginx <span class="nv">nginx</span><span class="o">=</span>nginx:787977da <span class="nt">--record</span>
<span class="c"># 查看历史版本</span>
kubectl rollout <span class="nb">history </span>deploy nginx
<span class="c"># 回滚到上一个版本</span>
kubectl rollout undo deploy nginx
<span class="c"># 进行多次更新</span>
kubectl <span class="nb">set </span>image deploy nginx <span class="nv">nginx</span><span class="o">=</span>nginx:787977da <span class="nt">--record</span>
kubectl <span class="nb">set </span>image deploy nginx <span class="nv">nginx</span><span class="o">=</span>nginx:787977dadaa <span class="nt">--record</span>
kubectl <span class="nb">set </span>image deploy nginx <span class="nv">nginx</span><span class="o">=</span>nginx:787977xxxxxdadaa <span class="nt">--record</span>
kubectl <span class="nb">set </span>image deploy nginx <span class="nv">nginx</span><span class="o">=</span>nginx:787977dadxxxxxdadaa <span class="nt">--record</span>
<span class="c"># 查看历史记录</span>
kubectl  rollout <span class="nb">history </span>deploy nginx
<span class="c"># 查看指定版本的详细信息</span>
kubectl rollout <span class="nb">history </span>deploy nginx <span class="nt">--revision</span><span class="o">=</span>5
<span class="c"># 回滚到执行的版本</span>
kubectl rollout undo deploy nginx <span class="nt">--to-revision</span><span class="o">=</span>5
<span class="c"># 查看deploy状态</span>
kubectl get deploy <span class="nt">-oyaml</span>
</code></pre></div></div> <h3 id="15-deployment-的暂停和恢复">1.5 Deployment 的暂停和恢复</h3> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Deployment 暂停功能</span>
kubectl rollout pause deployment nginx
kubectl <span class="nb">set </span>image deploy nginx <span class="nv">nginx</span><span class="o">=</span>nginx:1.15.3 <span class="nt">--record</span>
<span class="c"># 进行第二次配置变更</span>
<span class="c"># 添加内存CPU配置</span>
kubectl <span class="nb">set </span>resources deploy nginx <span class="nt">-c</span> nginx <span class="nt">--limits</span><span class="o">=</span><span class="nv">cpu</span><span class="o">=</span>200m,memory<span class="o">=</span>128Mi <span class="nt">--requests</span><span class="o">=</span><span class="nv">cpu</span><span class="o">=</span>10m,memory<span class="o">=</span>16Mi
<span class="c"># 查看pod是否被更新</span>
kubectl get po
kubectl rollout resume deploy nginx
kubectl get rs
</code></pre></div></div> <h3 id="16-deployment-注意事项">1.6 Deployment 注意事项</h3> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># kubectl get deploy nginx -oyaml</span>
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: <span class="s2">"12"</span>
    kubernetes.io/change-cause:
      kubectl <span class="nb">set </span>image deploy nginx <span class="nv">nginx</span><span class="o">=</span>nginx:1.15.3
      <span class="nt">--record</span><span class="o">=</span><span class="nb">true
  </span>creationTimestamp: <span class="s2">"2020-09-19T02:41:11Z"</span>
  generation: 19
  labels:
    app: nginx
  name: nginx
  namespace: default
spec:
  progressDeadlineSeconds: 600
  replicas: 2
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: nginx
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    <span class="nb">type</span>: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: nginx
    spec:
      containers:
        - image: nginx:1.15.3
          imagePullPolicy: IfNotPresent
          name: nginx
          resources:
            limits:
              cpu: 200m
              memory: 128Mi
            requests:
              cpu: 10m
              memory: 16Mi
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: <span class="o">{}</span>
      terminationGracePeriodSeconds: 30
</code></pre></div></div> <ul> <li>spec.revisionHistoryLimit：设置保留 RS 旧的 revision 的个数，设置为 0 的话，不保留历史数据</li> <li>spec.minReadySeconds：可选参数，指定新创建的 Pod 在没有任何容器崩溃的情况下视为 Ready 最小的秒数，默认为 0，即一旦被创建就视为可用。</li> <li>滚动更新的策略： <ul> <li>spec.strategy.type：更新 deployment 的方式，默认是 RollingUpdate <ul> <li>RollingUpdate：滚动更新，可以指定 maxSurge 和 maxUnavailable <ul> <li>maxUnavailable：指定在回滚或更新时最大不可用的 Pod 的数量，可选字段，默认 25%，可以设置成数字或百分比，如果该值为 0，那么 maxSurge 就不能 0</li> <li>maxSurge：可以超过期望值的最大 Pod 数，可选字段，默认为 25%，可以设置成数字或百分比，如果该值为 0，那么 maxUnavailable 不能为 0</li> </ul> </li> <li>Recreate：重建，先删除旧的 Pod，在创建新的 Pod</li> </ul> </li> </ul> </li> </ul> <h2 id="第五章-serviceingress">第五章 Service&amp;Ingress</h2> <h3 id="使用pod-ip-访问应用的问题">使用Pod IP 访问应用的问题</h3> <p>Service： 主要用于Pod之间的通信，相对于Pod的IP它创建完成以后就是不变的资源。 Namespace级别的隔离。</p> <h3 id="最常用的service">最常用的service</h3> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">my-service</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">demo-nginx</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">80</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="m">80</span>
      <span class="na">name</span><span class="pi">:</span> <span class="s">http</span>
</code></pre></div></div> <h3 id="service的类型">Service的类型</h3> <p>ClusterIP：在集群内部使用的，默认类型 NodePort：在每个宿主机上暴露一个随机端口，30000-32767，–service-node-port-range，集群外部可访问。 LoadBalancer：使用云服务商提供的IP地址。成本太高。 ExternalName：反代到指定的域名上。</p> <p>没有Selector的service。不会自动创建EndPoints。 192.168.1.100 3306 没有selector的service的名字+端口进行访问到192.168.1.100 3306。 172.16.1.100</p> <p>ClusterIP+Ingress 域名访问</p> <h3 id="ingress">Ingress</h3> <p>它是Kubernetes集群中服务的入口，可以提供负载均衡、SSL终止和基于域名的虚拟主机。Treafik、Nginx、HAProxy、Istio。</p> <p>Ingress 官方文档：https://kubernetes.io/docs/concepts/services-networking/ingress/</p> <p>Ingress-Nginx安装文档：https://kubernetes.github.io/ingress-nginx/deploy/</p> <p>Ingress-Nginx 文档：https://kubernetes.github.io/ingress-nginx/user-guide/nginx-configuration/</p> <p>定义一个Ingress：</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">networking.k8s.io/v1beta1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Ingress</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">simple-fanout-example</span>
  <span class="na">annotations</span><span class="pi">:</span>
    <span class="na">nginx.ingress.kubernetes.io/rewrite-target</span><span class="pi">:</span> <span class="s">/</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">rules</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">host</span><span class="pi">:</span> <span class="s">foo.bar.com</span>
      <span class="na">http</span><span class="pi">:</span>
        <span class="na">paths</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="na">path</span><span class="pi">:</span> <span class="s">/foo</span>
            <span class="na">backend</span><span class="pi">:</span>
              <span class="na">serviceName</span><span class="pi">:</span> <span class="s">service1</span>
              <span class="na">servicePort</span><span class="pi">:</span> <span class="m">4200</span>
          <span class="pi">-</span> <span class="na">path</span><span class="pi">:</span> <span class="s">/bar</span>
            <span class="na">backend</span><span class="pi">:</span>
              <span class="na">serviceName</span><span class="pi">:</span> <span class="s">service2</span>
              <span class="na">servicePort</span><span class="pi">:</span> <span class="m">8080</span>
</code></pre></div></div> <p>host: 可选参数，一般都会配置我们自己的域名。 path: 一个路径对应一个serviceName和一个Port backend: path对应的后端是谁。 foo.bar.com/foo service1:4200</p> <p>Ingress：https://kubernetes.io/docs/concepts/services-networking/ingress/</p> <h2 id="第六章-hpa-configmap-secret">第六章 HPA-ConfigMap Secret</h2> <p>HPA Horizontal Pod Autoscaler：Pod的水平自动伸缩器。 观察Pod的CPU、内存使用率自动扩展或缩容Pod的数量。 不适用于无法缩放的对象，比如DaemonSet。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CPU、内存
自定义指标的扩缩容。
</code></pre></div></div> <p>必须定义 Requests参数，必须安装metrics-server。</p> <p>kubectl autoscale deploy demo-nginx –cpu-percent=20 –min=2 –max=5</p> <p>ConfigMap： 一般用ConfigMap去管理一些配置文件、或者一些大量的环境变量信息。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ConfigMap将配置和Pod分开，有一个nginx，nginx.conf -&gt; configmap,nginx
更易于配置文件的更改和管理。

Secret：Secret更倾向于存储和共享敏感、加密的配置信息。
</code></pre></div></div> <p>ConfigMap中文地址：https://kubernetes.io/zh/docs/tasks/configure-pod-container/configure-pod-configmap/</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">dapi-test-pod</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">nodeName</span><span class="pi">:</span> <span class="s">k8s-master02</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">test-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">busybox:1.28</span>
      <span class="na">imagePullPolicy</span><span class="pi">:</span> <span class="s">IfNotPresent</span>
      <span class="na">command</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">/bin/sh"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">-c"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">env"</span><span class="pi">]</span>
      <span class="na">envFrom</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">configMapRef</span><span class="pi">:</span>
            <span class="na">name</span><span class="pi">:</span> <span class="s">special-config</span>
      <span class="na">env</span><span class="pi">:</span>
        <span class="c1"># Define the environment variable</span>
        <span class="c1">#- name: SPECIAL_LEVEL_KEY</span>
        <span class="c1">#  valueFrom:</span>
        <span class="c1">#    configMapKeyRef:</span>
        <span class="c1">#      # The ConfigMap containing the value you want to assign to SPECIAL_LEVEL_KEY</span>
        <span class="c1">#      name: special-config</span>
        <span class="c1">#      # Specify the key associated with the value</span>
        <span class="c1">#      key: special.how</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">test</span>
          <span class="na">value</span><span class="pi">:</span> <span class="s">test-value</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">mysqlHostAddress</span>
          <span class="na">value</span><span class="pi">:</span> <span class="s">10.10.10.10</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">mysqlPort</span>
          <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">3306"</span> <span class="c1"># only string</span>
  <span class="na">restartPolicy</span><span class="pi">:</span> <span class="s">Never</span>
</code></pre></div></div> <p>把ConfigMap挂载到容器中充当配置文件：</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">dapi-test-pod</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">nodeName</span><span class="pi">:</span> <span class="s">k8s-master02</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">test-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">busybox:1.28</span>
      <span class="na">imagePullPolicy</span><span class="pi">:</span> <span class="s">IfNotPresent</span>
      <span class="na">command</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">/bin/sh"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">-c"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">sleep</span><span class="nv"> </span><span class="s">3600"</span><span class="pi">]</span>
      <span class="na">volumeMounts</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">config-volume</span>
          <span class="na">mountPath</span><span class="pi">:</span> <span class="s">/mnt/1</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">contain-filename</span>
          <span class="na">mountPath</span><span class="pi">:</span> <span class="s">/mnt/2</span>
      <span class="na">envFrom</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">configMapRef</span><span class="pi">:</span>
            <span class="na">name</span><span class="pi">:</span> <span class="s">special-config</span>
      <span class="na">env</span><span class="pi">:</span>
        <span class="c1"># Define the environment variable</span>
        <span class="c1">#- name: SPECIAL_LEVEL_KEY</span>
        <span class="c1">#  valueFrom:</span>
        <span class="c1">#    configMapKeyRef:</span>
        <span class="c1">#      # The ConfigMap containing the value you want to assign to SPECIAL_LEVEL_KEY</span>
        <span class="c1">#      name: special-config</span>
        <span class="c1">#      # Specify the key associated with the value</span>
        <span class="c1">#      key: special.how</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">test</span>
          <span class="na">value</span><span class="pi">:</span> <span class="s">test-value</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">mysqlHostAddress</span>
          <span class="na">value</span><span class="pi">:</span> <span class="s">10.10.10.10</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">mysqlPort</span>
          <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">3306"</span> <span class="c1"># only string</span>
  <span class="na">restartPolicy</span><span class="pi">:</span> <span class="s">Never</span>
  <span class="na">volumes</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">config-volume</span>
      <span class="na">configMap</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">special-config</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">contain-filename</span>
      <span class="na">configMap</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">game-ui-cm-custom-name</span>
</code></pre></div></div> <p>Secret： 用来保存敏感信息的，比如密码、令牌或者Key，Redis、MySQL密码。</p> <p>$ * \ 特殊字符单引号无需转义</p> <p>Secret用途： ImagePullSecret：Pod拉取私有镜像仓库时使用的账户密码，里面的账户信息，会传递给kubelet，然后kubelet就可以拉取有密码的仓库里面的镜像。</p> <p>创建一个docker registry的secret [root@k8s-master01 secret]# kubectl create secret docker-registry docker-secret2 –docker-server=hub.docker.com –docker-username=DOCKER_USER –docker-password=DOCKER_PASSWORD –docker-email=DOCKER_EMAIL secret/docker-secret2 created</p> <p>Secret 中文文档：https://kubernetes.io/zh/docs/concepts/configuration/secret/</p> <p>subPath解决目录覆盖的问题：</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">annotations</span><span class="pi">:</span>
    <span class="na">deployment.kubernetes.io/revision</span><span class="pi">:</span> <span class="s2">"</span><span class="s">4"</span>
  <span class="na">creationTimestamp</span><span class="pi">:</span> <span class="s2">"</span><span class="s">2020-03-16T14:36:14Z"</span>
  <span class="na">generation</span><span class="pi">:</span> <span class="m">5</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">demo-nginx</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">demo-nginx</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">default</span>
  <span class="na">resourceVersion</span><span class="pi">:</span> <span class="s2">"</span><span class="s">1338515"</span>
  <span class="na">selfLink</span><span class="pi">:</span> <span class="s">/apis/apps/v1/namespaces/default/deployments/demo-nginx</span>
  <span class="na">uid</span><span class="pi">:</span> <span class="s">ca0c135f-31a3-464c-b1f2-10f88e9f1c90</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">progressDeadlineSeconds</span><span class="pi">:</span> <span class="m">600</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">2</span>
  <span class="na">revisionHistoryLimit</span><span class="pi">:</span> <span class="m">10</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">demo-nginx</span>
  <span class="na">strategy</span><span class="pi">:</span>
    <span class="na">rollingUpdate</span><span class="pi">:</span>
      <span class="na">maxSurge</span><span class="pi">:</span> <span class="s">25%</span>
      <span class="na">maxUnavailable</span><span class="pi">:</span> <span class="m">1</span>
    <span class="na">type</span><span class="pi">:</span> <span class="s">RollingUpdate</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">creationTimestamp</span><span class="pi">:</span> <span class="kc">null</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">demo-nginx</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">command</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="s">sh</span>
            <span class="pi">-</span> <span class="s">-c</span>
            <span class="pi">-</span> <span class="s">sleep </span><span class="m">3600</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
          <span class="na">imagePullPolicy</span><span class="pi">:</span> <span class="s">IfNotPresent</span>
          <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
          <span class="na">ports</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">80</span>
              <span class="na">name</span><span class="pi">:</span> <span class="s">web</span>
              <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
          <span class="na">resources</span><span class="pi">:</span>
            <span class="na">limits</span><span class="pi">:</span>
              <span class="na">cpu</span><span class="pi">:</span> <span class="s">100m</span>
              <span class="na">memory</span><span class="pi">:</span> <span class="s">270Mi</span>
            <span class="na">requests</span><span class="pi">:</span>
              <span class="na">cpu</span><span class="pi">:</span> <span class="s">100m</span>
              <span class="na">memory</span><span class="pi">:</span> <span class="s">70Mi</span>
          <span class="na">terminationMessagePath</span><span class="pi">:</span> <span class="s">/dev/termination-log</span>
          <span class="na">terminationMessagePolicy</span><span class="pi">:</span> <span class="s">File</span>
          <span class="na">volumeMounts</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">mountPath</span><span class="pi">:</span> <span class="s">/etc/nginx/nginx.conf</span>
              <span class="na">name</span><span class="pi">:</span> <span class="s">config-volume</span>
              <span class="na">subPath</span><span class="pi">:</span> <span class="s">etc/nginx/nginx.conf</span>
      <span class="na">dnsPolicy</span><span class="pi">:</span> <span class="s">ClusterFirst</span>
      <span class="na">restartPolicy</span><span class="pi">:</span> <span class="s">Always</span>
      <span class="na">schedulerName</span><span class="pi">:</span> <span class="s">default-scheduler</span>
      <span class="na">securityContext</span><span class="pi">:</span> <span class="pi">{}</span>
      <span class="na">terminationGracePeriodSeconds</span><span class="pi">:</span> <span class="m">30</span>
      <span class="na">volumes</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">configMap</span><span class="pi">:</span>
            <span class="na">defaultMode</span><span class="pi">:</span> <span class="m">420</span>
            <span class="na">items</span><span class="pi">:</span>
              <span class="pi">-</span> <span class="na">key</span><span class="pi">:</span> <span class="s">nginx.conf</span>
                <span class="na">path</span><span class="pi">:</span> <span class="s">etc/nginx/nginx.conf</span>
            <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-conf</span>
          <span class="na">name</span><span class="pi">:</span> <span class="s">config-volume</span>
</code></pre></div></div> <p>ConfigMap和Secret如果是以subPath 的形式挂载的，那么Pod是不会感知到ConfigMap和Secret的更新的。 如果Pod的变量来自于ConfigMap和Secret中定义的内容，那么ConfigMap和Secret更新后，也不会更新Pod中的变量。</p> <p>postStart：容器启动之前执行的命令 preStop：容器停止之前执行的命令</p> <p>热更新ConfigMap或Secret： [root@k8s-master01 ~]# kubectl create cm nginx-conf –from-file=nginx.conf –dry-run -oyaml | kubectl replace -f-</p>]]></content><author><name></name></author><category term="kubernetes"/><summary type="html"><![CDATA[kubernetes]]></summary></entry><entry><title type="html">ubuntu 18.04.4 安装JDK</title><link href="https://quaner-dev.github.io/blog/2024/Ubuntu-18.04.4-%E5%AE%89%E8%A3%85JDK/" rel="alternate" type="text/html" title="ubuntu 18.04.4 安装JDK"/><published>2024-05-01T00:00:00+00:00</published><updated>2024-05-01T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2024/Ubuntu%2018.04.4%20%E5%AE%89%E8%A3%85JDK</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2024/Ubuntu-18.04.4-%E5%AE%89%E8%A3%85JDK/"><![CDATA[<h1 id="ubuntu-18044-安装jdk">Ubuntu 18.04.4 安装JDK</h1> <h2 id="下载二进制jdk">下载二进制JDK</h2> <p><a href="https://www.oracle.com/java/technologies/downloads/#java8">官网下载地址</a> 下载对应的按转包</p> <h2 id="放置到对用户目录">放置到对用户目录</h2> <p>mkdir /usr/lib/jdk tar xvf jdk-8u411-linux64.tar.gz -C /usr/lib/jdk</p> <h2 id="添加环境变量">添加环境变量</h2> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vim /etc/profile
</code></pre></div></div> <p>在文件末尾增加</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># java env</span>
<span class="nb">export </span><span class="nv">JAVA_HOME</span><span class="o">=</span>/usr/lib/jdk/jdk1.8.0_411
<span class="nb">export </span><span class="nv">JRE_HOME</span><span class="o">=</span><span class="k">${</span><span class="nv">JAVA_HOME</span><span class="k">}</span>/jre
<span class="nb">export </span><span class="nv">CLASSPATH</span><span class="o">=</span>.:<span class="k">${</span><span class="nv">JAVA_HOME</span><span class="k">}</span>/lib:<span class="k">${</span><span class="nv">JRE_HOME</span><span class="k">}</span>/lib
<span class="nb">export </span><span class="nv">PATH</span><span class="o">=</span><span class="k">${</span><span class="nv">JAVA_HOME</span><span class="k">}</span>/bin:<span class="nv">$PATH</span>
</code></pre></div></div> <h2 id="生效环境变量">生效环境变量</h2> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">source</span> /etc/profile
</code></pre></div></div> <h2 id="测试java命令">测试java命令</h2> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>java <span class="nt">-version</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="JDK"/><summary type="html"><![CDATA[正则表达式记录]]></summary></entry><entry><title type="html">二进制部署Docker</title><link href="https://quaner-dev.github.io/blog/2024/%E4%BA%8C%E8%BF%9B%E5%88%B6%E9%83%A8%E7%BD%B2Docker/" rel="alternate" type="text/html" title="二进制部署Docker"/><published>2024-05-01T00:00:00+00:00</published><updated>2024-05-01T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2024/%E4%BA%8C%E8%BF%9B%E5%88%B6%E9%83%A8%E7%BD%B2Docker</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2024/%E4%BA%8C%E8%BF%9B%E5%88%B6%E9%83%A8%E7%BD%B2Docker/"><![CDATA[<h1 id="二进制部署docker">二进制部署Docker</h1> <h2 id="install-static-binaries">Install static binaries</h2> <ol> <li> <p>Download the static binary archive. Go to https://download.docker.com/linux/static/stable/, choose your hardware platform, and download the <code class="language-plaintext highlighter-rouge">.tgz</code> file relating to the version of Docker Engine you want to install.</p> </li> <li> <p>Extract the archive using the <code class="language-plaintext highlighter-rouge">tar</code> utility. The <code class="language-plaintext highlighter-rouge">dockerd</code> and <code class="language-plaintext highlighter-rouge">docker</code> binaries are extracted.</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">tar </span>xzvf /path/to/FILE.tar.gz
</code></pre></div> </div> </li> <li> <p>Optional: Move the binaries to a directory on your executable path, such as <code class="language-plaintext highlighter-rouge">/usr/bin/</code>. If you skip this step, you must provide the path to the executable when you invoke <code class="language-plaintext highlighter-rouge">docker</code> or <code class="language-plaintext highlighter-rouge">dockerd</code> commands.</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo cp </span>docker/<span class="k">*</span> /usr/bin/
</code></pre></div> </div> </li> <li> <p>Start the Docker daemon:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>dockerd &amp;
</code></pre></div> </div> <p>If you need to start the daemon with additional options, modify the above command accordingly or create and edit the file <code class="language-plaintext highlighter-rouge">/etc/docker/daemon.json</code> to add the custom configuration options.</p> </li> <li> <p>Verify that Docker is installed correctly by running the <code class="language-plaintext highlighter-rouge">hello-world</code> image.</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker run hello-world
</code></pre></div> </div> <p>This command downloads a test image and runs it in a container. When the container runs, it prints a message and exits.</p> </li> </ol> <p>You have now successfully installed and started Docker Engine.</p> <h2 id="configure-docker-to-start-on-boot-with-systemd">Configure Docker to start on boot with systemd</h2> <p>Many modern Linux distributions use <a href="https://docs.docker.com/config/daemon/systemd/">systemd</a> to manage which services start when the system boots. On Debian and Ubuntu, the Docker service starts on boot by default. To automatically start Docker and containerd on boot for other Linux distributions using systemd, run the following commands:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>```shell
sudo systemctl enable docker.service
sudo systemctl enable containerd.service
```
</code></pre></div></div> <p>To stop this behavior, use <code class="language-plaintext highlighter-rouge">disable</code> instead.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>```shell
sudo systemctl disable docker.service
sudo systemctl disable containerd.service
```
</code></pre></div></div> <p>If you need to add an HTTP proxy, set a different directory or partition for the Docker runtime files, or make other customizations, see <a href="https://docs.docker.com/config/daemon/systemd/">customize your systemd Docker daemon options</a>.</p> <h2 id="manually-create-the-systemd-unit-files">Manually create the systemd unit files</h2> <p>When installing the binary without a package manager, you may want to integrate Docker with systemd. For this, install the two unit files (service and socket) from <a href="https://github.com/moby/moby/tree/master/contrib/init/systemd">the GitHub repository</a> to /etc/systemd/system.</p> <h3 id="systemd-unit-files">systemd unit files</h3> <ul> <li><a href="https://github.com/moby/moby/blob/master/contrib/init/systemd/docker.service">docker.service</a></li> <li><a href="https://github.com/moby/moby/blob/master/contrib/init/systemd/docker.socket">docker.socket</a></li> </ul>]]></content><author><name></name></author><category term="Docker"/><summary type="html"><![CDATA[二进制部署Docker]]></summary></entry><entry><title type="html">配置聚合网口</title><link href="https://quaner-dev.github.io/blog/2024/%E9%85%8D%E7%BD%AE%E8%81%9A%E5%90%88%E7%BD%91%E5%8F%A3/" rel="alternate" type="text/html" title="配置聚合网口"/><published>2024-05-01T00:00:00+00:00</published><updated>2024-05-01T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2024/%E9%85%8D%E7%BD%AE%E8%81%9A%E5%90%88%E7%BD%91%E5%8F%A3</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2024/%E9%85%8D%E7%BD%AE%E8%81%9A%E5%90%88%E7%BD%91%E5%8F%A3/"><![CDATA[<h1 id="配置聚合网口">配置聚合网口</h1> <h2 id="要求">要求</h2> <p>完成 OS 层面安装，进入到终端界面执行如下命令</p> <h2 id="修改配置文件">修改配置文件</h2> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vim /etc/netplan/01-netcfg.yaml
</code></pre></div></div> <h2 id="在文件末尾中增加内容结果如下">在文件末尾中增加内容，结果如下</h2> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="s">network：</span>
  <span class="s">version</span><span class="err">:</span> <span class="m">2</span>
  <span class="na">renderer</span><span class="pi">:</span> <span class="s">networkd</span>

  <span class="na">ethernets</span><span class="pi">:</span>
    <span class="na">eno1</span><span class="pi">:</span> <span class="pi">{}</span> <span class="c1"># eno1代表网口名称，可以使用ifconfig -a查出网口名称，如果需要确认是哪个网口，可以使用ethtool -p [网口名称]，可以在点亮物理机上网口的灯</span>
    <span class="na">eno2</span><span class="pi">:</span> <span class="pi">{}</span>

  <span class="na">bonds</span><span class="pi">:</span>
    <span class="na">bond0</span><span class="pi">:</span>
      <span class="na">addresses</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">xx.xx.xx.xx/24</span><span class="pi">]</span> <span class="c1"># 这里的xx.xx.xx.xx代表服务器IP，24代表掩码位，例如掩码位24对应子网掩码255.255.255.0</span>
      <span class="na">gateway4</span><span class="pi">:</span> <span class="s">xx.xx.xx.xx</span> <span class="c1"># 这里的xx.xx.xx.xx代表网关IP</span>
      <span class="na">interfaces</span><span class="pi">:</span> <span class="c1"># interfaces代表bond绑定的网口，填写的网口名称与ethernets中所填写的网口名称相同即可</span>
        <span class="pi">-</span> <span class="s">eno1</span>
        <span class="pi">-</span> <span class="s">eno2</span>
      <span class="na">parameters</span><span class="pi">:</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="s">balance-rr</span> <span class="c1"># bond模式，模式种类如下章描述，根据需求调整参数即可</span>
        <span class="na">mii-monitor-interval</span><span class="pi">:</span> <span class="m">100</span>
</code></pre></div></div> <h2 id="bond-模式">bond 模式</h2> <table> <thead> <tr> <th>mode</th> <th>参数</th> <th>名称</th> </tr> </thead> <tbody> <tr> <td>mode=0</td> <td>balance-rr</td> <td>轮询均衡模式</td> </tr> <tr> <td>mode=1</td> <td>active-backup</td> <td>主备模式</td> </tr> <tr> <td>mode=2</td> <td>balance-xor</td> <td>平衡策略</td> </tr> <tr> <td>mode=3</td> <td>broadcast</td> <td>广播策略</td> </tr> <tr> <td>mode=4</td> <td>802.3ad</td> <td> </td> </tr> <tr> <td>mode=5</td> <td>balance-tlb</td> <td> </td> </tr> <tr> <td>mode=6</td> <td>balance-alb</td> <td> </td> </tr> </tbody> </table>]]></content><author><name></name></author><category term="bond"/><summary type="html"><![CDATA[配置聚合网口]]></summary></entry><entry><title type="html">常用正则表达式记录</title><link href="https://quaner-dev.github.io/blog/2024/%E5%B8%B8%E7%94%A8%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F%E8%AE%B0%E5%BD%95/" rel="alternate" type="text/html" title="常用正则表达式记录"/><published>2024-03-19T00:00:00+00:00</published><updated>2024-03-19T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2024/%E5%B8%B8%E7%94%A8%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F%E8%AE%B0%E5%BD%95</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2024/%E5%B8%B8%E7%94%A8%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F%E8%AE%B0%E5%BD%95/"><![CDATA[<h1 id="常用正则表达式记录">常用正则表达式记录</h1> <h2 id="身份证">身份证</h2> <pre><code class="language-regex">[1-9]\d{5}(19|20)\d{2}((0[1-9])|(1[0-2]))(([0-2][1-9])|10|20|30|31)\d{3}[Xx\d]
</code></pre> <h2 id="中文">中文</h2> <pre><code class="language-txt">[\u4e00-\u9fa5]
</code></pre>]]></content><author><name></name></author><category term="regex"/><summary type="html"><![CDATA[正则表达式记录]]></summary></entry><entry><title type="html">LVM简介</title><link href="https://quaner-dev.github.io/blog/2024/LVM/" rel="alternate" type="text/html" title="LVM简介"/><published>2024-03-09T00:00:00+00:00</published><updated>2024-03-09T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2024/LVM</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2024/LVM/"><![CDATA[<h1 id="lvm简介">LVM简介</h1> <h2 id="lvm概念">LVM概念</h2> <h3 id="pv">PV</h3> <p>PV（Physical Volume）：物理卷，即实际存在的硬盘，分区或者RAID</p> <h3 id="vg">VG</h3> <p>VG（Volume Group）：卷组，由多个物理卷组合成的整体卷组</p> <h3 id="lv">LV</h3> <p>LV（Logical Volume）：逻辑卷，从卷组中分割出来，可以挂载的逻辑存储卷</p> <h2 id="lvm命令">LVM命令</h2> <table> <thead> <tr> <th>功能</th> <th>物理卷管理</th> <th>卷组管理</th> <th>逻辑卷管理</th> </tr> </thead> <tbody> <tr> <td>扫描</td> <td>pvscan</td> <td>vgscan</td> <td>lvscan</td> </tr> <tr> <td>建立</td> <td>pvcreate</td> <td>vgcreate</td> <td>lvcreate</td> </tr> <tr> <td>显示</td> <td>pvdisplay</td> <td>vgdisplay</td> <td>lvdisplay</td> </tr> <tr> <td>删除</td> <td>pvremote</td> <td>vgremote</td> <td>lvremove</td> </tr> <tr> <td>扩展</td> <td>物理卷无法扩展</td> <td>vgextend</td> <td>lvextend</td> </tr> <tr> <td>缩小</td> <td>物理卷无法缩小</td> <td>vgreduce</td> <td>lvreduce</td> </tr> </tbody> </table> <h2 id="lvm实战">LVM实战</h2> <h3 id="pvcreate">pvcreate</h3> <p><img src="/assets/img/blog_preview/2024-03-09-LVM/pvcreate.png" alt="pvcreate"/></p> <h3 id="pvremove">pvremove</h3> <p><img src="/assets/img/blog_preview/2024-03-09-LVM/pvremove.png" alt="pvremove"/></p> <h3 id="vgcreate">vgcreate</h3> <p><img src="/assets/img/blog_preview/2024-03-09-LVM/vgcreate.png" alt="vgcreate"/></p> <h3 id="vgremove">vgremove</h3> <p><img src="/assets/img/blog_preview/2024-03-09-LVM/vgremove.png" alt="vgremove"/></p> <h3 id="lvcreate">lvcreate</h3> <p><img src="/assets/img/blog_preview/2024-03-09-LVM/lvcreate.png" alt="lvcreate"/></p> <h3 id="lvremove">lvremove</h3> <p><img src="/assets/img/blog_preview/2024-03-09-LVM/lvremove.png" alt="lvremove"/></p>]]></content><author><name></name></author><category term="LVM"/><summary type="html"><![CDATA[介绍LVM使用]]></summary></entry><entry><title type="html">检查硬件情况</title><link href="https://quaner-dev.github.io/blog/2024/%E6%A3%80%E6%9F%A5%E7%A1%AC%E4%BB%B6%E6%83%85%E5%86%B5/" rel="alternate" type="text/html" title="检查硬件情况"/><published>2024-01-16T00:00:00+00:00</published><updated>2024-01-16T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2024/%E6%A3%80%E6%9F%A5%E7%A1%AC%E4%BB%B6%E6%83%85%E5%86%B5</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2024/%E6%A3%80%E6%9F%A5%E7%A1%AC%E4%BB%B6%E6%83%85%E5%86%B5/"><![CDATA[<h1 id="检查硬件情况">检查硬件情况</h1> <h2 id="cpu">CPU</h2> <h3 id="cpu检查命令">CPU检查命令</h3> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lscpu
</code></pre></div></div> <h3 id="lscpu结果解释">lscpu结果解释</h3> <table> <thead> <tr> <th>结果</th> <th>解释</th> </tr> </thead> <tbody> <tr> <td>Architecture</td> <td>架构</td> </tr> <tr> <td>CPU op-mode(s)</td> <td>运行方式</td> </tr> <tr> <td>Byte Order</td> <td>字节顺序</td> </tr> <tr> <td>CPU(s)</td> <td>逻辑cpu数</td> </tr> <tr> <td>On-line CPU(s) list</td> <td>在线cpu</td> </tr> <tr> <td>Thread(s) per core</td> <td>每个核包含线程数（在linux中线程等于逻辑cpu，即运行程序的最小物理单元）</td> </tr> <tr> <td>Core(s) per socket</td> <td>每个CPU插槽（或物理CPU器件）包含核心数</td> </tr> <tr> <td>Socket(s)</td> <td>主板上CPU插槽数（或物理CPU器件数量）</td> </tr> <tr> <td>NUMA node(s)</td> <td>代表内存架构，是Non-uniform memory access的缩写，这里代表2个内存控制器，但是NUMA node和socket不是直接对应的，具体布局见最后两个参数，每个主板的布局可能不同</td> </tr> <tr> <td>Vendor ID</td> <td>CPU厂商ID</td> </tr> <tr> <td>CPU family</td> <td>CPU系列</td> </tr> <tr> <td>Model</td> <td>型号编号</td> </tr> <tr> <td>Model name</td> <td>型号名称</td> </tr> <tr> <td>Stepping</td> <td>步进</td> </tr> <tr> <td>CPU MHz</td> <td>默认主频</td> </tr> <tr> <td>CPU max MHz</td> <td>最大超频</td> </tr> <tr> <td>CPU min MHz</td> <td>最小频率</td> </tr> <tr> <td>Virtualization</td> <td>CPU支持的虚拟化技术</td> </tr> <tr> <td>L1d cache</td> <td>一级数据缓存</td> </tr> <tr> <td>L1i cache</td> <td>一级指令缓存</td> </tr> <tr> <td>L2 cache</td> <td>二级缓存</td> </tr> <tr> <td>L3 cache</td> <td>三级缓存</td> </tr> <tr> <td>NUMA node0 CPU(s)</td> <td>逻辑cpu0-28和48-71在NUMA node0上</td> </tr> <tr> <td>NUMA node1 CPU(s)</td> <td>逻辑cpu24-47和72-95在NUMA node1上</td> </tr> </tbody> </table> <h2 id="查看系统信息">查看系统信息</h2> <h3 id="系统检查命令">系统检查命令</h3> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> /etc/os-release
</code></pre></div></div> <h3 id="结果解释">结果解释</h3> <table> <thead> <tr> <th>结果</th> <th>解释</th> </tr> </thead> <tbody> <tr> <td>NAME</td> <td>操作系统名称</td> </tr> <tr> <td>VERSION</td> <td>版本信息</td> </tr> <tr> <td>ID</td> <td>系统ID</td> </tr> <tr> <td>ID_LIKE</td> <td>类似系统ID</td> </tr> <tr> <td>PRETTY_NAME</td> <td>友好名称</td> </tr> <tr> <td>VERSION_ID</td> <td>版本ID</td> </tr> <tr> <td>HOME_URL</td> <td>官方网站链接</td> </tr> <tr> <td>SUPPORT URL</td> <td>技术支持链接</td> </tr> <tr> <td>BUG_REPORT_URL</td> <td>错误报告链接</td> </tr> <tr> <td>PRIVACY_POLICY_URL</td> <td>隐私政策链接</td> </tr> <tr> <td>VERSION_CODENAME</td> <td>版本代号</td> </tr> <tr> <td>UBUNTU_CODENAME</td> <td>UBUNTU版本代号</td> </tr> </tbody> </table> <h2 id="查看内存信息">查看内存信息</h2> <h3 id="free检查命令">free检查命令</h3> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>free <span class="nt">-h</span>
</code></pre></div></div> <h3 id="free结果解释">free结果解释</h3> <table> <thead> <tr> <th>结果</th> <th>解释</th> </tr> </thead> <tbody> <tr> <td>total</td> <td>表示系统的总内存</td> </tr> <tr> <td>used</td> <td>表示已经被应用程序使用的内存</td> </tr> <tr> <td>free</td> <td>表示当前还没有被使用的内存</td> </tr> <tr> <td>shared</td> <td>表示进程共享的内存</td> </tr> <tr> <td>buffers/cache</td> <td>表示被用作缓冲和缓存的内存。这部分内存是为了提高磁盘存取效率而被占用的，当应用程序需要使用内存时，这部分内存可以被快速回收</td> </tr> <tr> <td>Swap</td> <td>表示交换分区（swap space）的使用情况。当物理内存不足时，系统会使用交换分区来扩展可用内存</td> </tr> </tbody> </table> <h2 id="raid情况">Raid情况</h2> <h3 id="storcli64检查命令">storcli64检查命令</h3> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/opt/MegaRAID/storcli/storcli64 /c0 show all
</code></pre></div></div> <h3 id="storcli64结果解释">storcli64结果解释</h3> <table> <thead> <tr> <th> </th> <th> </th> </tr> </thead> <tbody> <tr> <td>Controller Name</td> <td>控制器的名称，例如 “MegaRAID SAS 9261-4i”</td> </tr> <tr> <td>Firmware Version</td> <td>控制器的固件版本号</td> </tr> <tr> <td>Serial Number</td> <td>控制器的序列号</td> </tr> <tr> <td>Physical Slot</td> <td>控制器所在的物理插槽位置</td> </tr> <tr> <td>PCI Bus/Device/Function</td> <td>控制器的PCI总线、设备、功能的详细信息</td> </tr> <tr> <td>Total Memory</td> <td>控制器上安装的总内存大小</td> </tr> <tr> <td>Free Memory</td> <td>当前未使用的内存大小</td> </tr> <tr> <td>Bus Speed</td> <td>总线的速度，例如 “6Gb/s”</td> </tr> <tr> <td>Device Count</td> <td>连接到控制器的设备数量</td> </tr> <tr> <td>Physical Disks</td> <td>显示所有连接到控制器的物理磁盘的信息，包括磁盘的型号、序列号、容量、状态等。</td> </tr> <tr> <td>Virtual Disks</td> <td>显示所有由控制器管理的虚拟磁盘的信息，包括RAID级别、状态、总容量、已用空间和可用空间等。</td> </tr> <tr> <td>Alarm and Event History</td> <td>显示有关控制器报警和历史事件的信息，例如故障磁盘、风扇问题等。</td> </tr> <tr> <td>Other Properties</td> <td>其他与控制器相关的属性，如最大连接设备数、最大传输大小等。</td> </tr> <tr> <td>Peripheral Information</td> <td>显示连接到控制器的其他外围设备的信息，例如连接的扩展卡、外部电缆等。(如果可用)</td> </tr> <tr> <td>Command Line Properties</td> <td>提供有关控制器命令行属性的信息，例如允许执行的命令等。(如果可用)</td> </tr> <tr> <td>OEM Information</td> <td>提供与特定制造商相关的附加信息。这可能包括特定的驱动程序信息、保修信息等。 (如果可用)</td> </tr> <tr> <td>Firmware Update Properties</td> <td>提供有关固件更新的信息，例如可用的固件版本、更新状态等。(如果可用)</td> </tr> <tr> <td>Controller Recovery and Boot Properties</td> <td>提供有关控制器恢复和引导属性的信息，例如启动顺序、故障转移设置等。(如果可用)</td> </tr> </tbody> </table> <h2 id="磁盘信息">磁盘信息</h2> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lsblk <span class="c"># 检查文件系统挂载情况</span>
<span class="nb">df</span> <span class="nt">-h</span> <span class="c"># 检查磁盘使用率</span>
</code></pre></div></div> <h2 id="磁盘读写速度测试">磁盘读写速度测试</h2> <h3 id="测试硬盘读速度">测试硬盘读速度</h3> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hdparm <span class="nt">-tT</span> /dev/sda
</code></pre></div></div> <h3 id="测试硬盘写速度">测试硬盘写速度</h3> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">dd </span><span class="k">if</span><span class="o">=</span>/dev/zero <span class="nv">of</span><span class="o">=</span>/tmp/test <span class="nv">bs</span><span class="o">=</span>50M <span class="nv">count</span><span class="o">=</span>50 <span class="nv">conv</span><span class="o">=</span>fdatasync <span class="c"># 修改of文件（outfile）用于测试不同硬盘写速度，并将生成的空文件删除</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="shell"/><summary type="html"><![CDATA[使用通用shell命令检查系统硬件配置]]></summary></entry><entry><title type="html">Iptables封闭端口</title><link href="https://quaner-dev.github.io/blog/2023/iptable%E5%B0%81%E9%97%AD%E7%AB%AF%E5%8F%A3/" rel="alternate" type="text/html" title="Iptables封闭端口"/><published>2023-12-26T00:00:00+00:00</published><updated>2023-12-26T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2023/iptable%E5%B0%81%E9%97%AD%E7%AB%AF%E5%8F%A3</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2023/iptable%E5%B0%81%E9%97%AD%E7%AB%AF%E5%8F%A3/"><![CDATA[<h1 id="iptables封闭端口">iptables封闭端口</h1> <h2 id="阻止所有外部ip地址访问特定端口">阻止所有外部IP地址访问特定端口</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>iptables <span class="nt">-A</span> INPUT <span class="nt">-p</span> tcp <span class="nt">--dport</span> &lt;端口号&gt; <span class="nt">-j</span> DROP
</code></pre></div></div> <h2 id="允许特定ip地址访问端口">允许特定IP地址访问端口</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>iptables <span class="nt">-A</span> INPUT <span class="nt">-p</span> tcp <span class="nt">-s</span> &lt;IP地址1&gt;,&lt;IP地址2&gt;,&lt;IP地址3&gt; <span class="nt">--dport</span> &lt;端口号&gt; <span class="nt">-j</span> ACCEPT
</code></pre></div></div> <h2 id="导出iptables规则">导出iptables规则</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>iptables-save <span class="o">&gt;</span> /root/iptables
</code></pre></div></div> <h2 id="保存iptables规则">保存iptables规则</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>iptables-restore &lt; /root/iptables
</code></pre></div></div>]]></content><author><name></name></author><category term="iptables"/><summary type="html"><![CDATA[Iptables封闭端口]]></summary></entry><entry><title type="html">Storcli使用文档</title><link href="https://quaner-dev.github.io/blog/2023/Storcli%E4%BD%BF%E7%94%A8%E6%96%87%E6%A1%A3/" rel="alternate" type="text/html" title="Storcli使用文档"/><published>2023-11-24T00:00:00+00:00</published><updated>2023-11-24T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2023/Storcli%E4%BD%BF%E7%94%A8%E6%96%87%E6%A1%A3</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2023/Storcli%E4%BD%BF%E7%94%A8%E6%96%87%E6%A1%A3/"><![CDATA[<h1 id="storcli使用文档">Storcli使用文档</h1> <h2 id="storcli安装地址">Storcli安装地址</h2> <p><a href="https://www.broadcom.com/site-search?q=Latest%20Storcli">Storcli安装包</a></p> <h2 id="操作手册">操作手册</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Ubuntu安装Storcli包</span>
dpkg <span class="nt">-i</span> xxx.deb

<span class="c"># Centos安装Storcli包</span>
rmp <span class="nt">-i</span> xxx.rpm

<span class="c"># 查看raid信息</span>
/opt/MegaRAID/storcli/storcli64 /c0 show all
</code></pre></div></div>]]></content><author><name></name></author><category term="raid"/><category term="storcli"/><summary type="html"><![CDATA[Storcli简介]]></summary></entry><entry><title type="html">Ubuntu下载离线安装包</title><link href="https://quaner-dev.github.io/blog/2023/ubuntu%E4%B8%8B%E8%BD%BD%E7%A6%BB%E7%BA%BF%E5%AE%89%E8%A3%85%E5%8C%85/" rel="alternate" type="text/html" title="Ubuntu下载离线安装包"/><published>2023-11-24T00:00:00+00:00</published><updated>2023-11-24T00:00:00+00:00</updated><id>https://quaner-dev.github.io/blog/2023/ubuntu%E4%B8%8B%E8%BD%BD%E7%A6%BB%E7%BA%BF%E5%AE%89%E8%A3%85%E5%8C%85</id><content type="html" xml:base="https://quaner-dev.github.io/blog/2023/ubuntu%E4%B8%8B%E8%BD%BD%E7%A6%BB%E7%BA%BF%E5%AE%89%E8%A3%85%E5%8C%85/"><![CDATA[<h1 id="ubuntu下载离线安装包">ubuntu下载离线安装包</h1> <h2 id="查询依赖列表">查询依赖列表</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 查询依赖列表</span>
<span class="c"># 替换&lt;packagename&gt;为需要安装的包</span>
<span class="nb">sudo </span>apt-cache depends <span class="nt">--recurse</span> <span class="nt">--no-recommends</span> <span class="nt">--no-suggests</span> <span class="nt">--no-conflicts</span> <span class="nt">--no-breaks</span> <span class="nt">--no-replaces</span> <span class="nt">--no-enhances</span> &lt;packagename&gt; | <span class="nb">grep</span> <span class="s2">"^</span><span class="se">\w</span><span class="s2">"</span> | <span class="nb">sort</span> <span class="nt">-u</span>
</code></pre></div></div> <h2 id="下载离线安装包">下载离线安装包</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 下载离线安装包</span>
<span class="c"># 替换&lt;packagename&gt;为需要安装的包</span>
<span class="nb">sudo </span>apt-get download <span class="si">$(</span>apt-cache depends <span class="nt">--recurse</span> <span class="nt">--no-recommends</span> <span class="nt">--no-suggests</span> <span class="nt">--no-conflicts</span> <span class="nt">--no-breaks</span> <span class="nt">--no-replaces</span> <span class="nt">--no-enhances</span> &lt;packagename&gt; | <span class="nb">grep</span> <span class="s2">"^</span><span class="se">\w</span><span class="s2">"</span> | <span class="nb">sort</span> <span class="nt">-u</span><span class="si">)</span>
</code></pre></div></div> <h2 id="下载安装包">下载安装包</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt <span class="nb">install</span> <span class="nt">-d</span> &lt;packagename&gt;
</code></pre></div></div>]]></content><author><name></name></author><category term="apt"/><summary type="html"><![CDATA[ubuntu下载离线安装包]]></summary></entry></feed>